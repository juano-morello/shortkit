slug: identity-membership
title: Identity, tenancy and membership
type: feature
phase: test
track: full
wave: 1        # waves 0 and 1 designed and approved; Test phase held on F-044
branch: feat/identity-membership
created: 2026-08-12
updated: 2026-08-13

# Roadmap item 1, opened 2026-08-12 after `foundation` closed and its retro residue was
# repaired. The roadmap words it: "An agency operator signs up, structures the agency into
# client workspaces, and invites a teammate scoped to specific workspaces."
#
# TRACK IS FORCED, NOT CHOSEN. config.yaml's force_full_paths covers apps/api/src/auth/**,
# apps/api/src/invitations/tokens/**, packages/contracts/src/auth/**, apps/api/drizzle/** and
# apps/api/src/tenancy/**. Any honest reading of item 1 touches at least three of those, so
# `full` is stated at the gate rather than offered as an option.
#
# SCOPE CUT AT THE REFINE GATE, not before it. Juano ruled 2026-08-12 to start from the whole
# of item 1 and let the one-shippable-increment check do the work, because `foundation` was
# re-scoped mid-flight after a pre-decided scope proved too big. The cut: THIS IS ITEM 1a -
# signup, session and workspaces. Invitations, capability tokens and mail are item 1b and keep
# the four carried obligations that belong to them.

gates:
  refine: { status: approved, approved_at: 2026-08-12, note: "Track full, FORCED by force_full_paths, stated rather than offered. Shippability check passes: built alone and stopped, an operator signs up and structures their agency, which is roadmap item 1s own first clause. Interview stopped at ~95% confidence after five questions; rollout, blast radius and data sensitivity were skipped because ADR-0030 and ADR-0028 already answer them. No blocking open questions." }
  plan:   { status: approved, approved_at: 2026-08-12, note: "One EPIC, 6 STORIEs, 17 TASKs, 36 ACs, 9 waves. THREE RULINGS AT THE GATE. (1) Step 7s second signal - wave 1 ships nothing - OVERRIDDEN as a misfire: the nine waves come from file serialisation (auth.config.ts, main.ts, migration ordering, the contracts barrel), not from batched increments, and the split that would satisfy the rule reinstates the API-only cut declined at Refine. Cost stated first: foundation was 10 TASKs at a 9:1 artifact-to-code ratio, this is 17. (2) SC-2 is discharged by TRANSPORT-LEVEL measurement; the criterion lost the words in a browser rather than keeping them unmeasured, and the limit is written onto SC-2 itself. (3) sdlc-design-auditor REJOINS the panel, trigger unchanged - dispatch on a diff touching apps/web/app/**, not on every TASK. scripts/** was NOT added to the ownership map and keeps routing through the ** fallback, ruled rather than overlooked." }
  design:
    wave_0: { status: approved, approved_at: 2026-08-13, note: "Approved together with wave 1 - wave 0 is TASK-018 alone and its design IS ADR-0050, written in this wave. Created at this gate, not at Plan: the role split arrived from F-024's measured account takeover and needed a TASK ahead of TASK-002 to create shortkit_auth before migration 0001 GRANTs to it." }
    wave_1_reapproved: { status: approved, approved_at: 2026-08-14, note: "SCOPED RE-APPROVAL for F-047 only. isolation-coverage.md was amended AFTER its freeze to carry a fourth context flag, app.membership_lookup_user, which wave 1's own design introduces via ADR-0045 and the contract's three-flag table did not hold. A2's carve-out widened with it; A4's predicate was verified BY EXECUTION to need no change, its app. branch being a prefix match; ISOLATION_EXCLUSIONS is a DIFFERENT list and stays at three. Nothing else in the contract changed. FOUND BY THE CONTROL F-044 WAS SPENT ALIGNING WITH THAT CONTRACT, on its first real run." }
    wave_1: { status: approved, approved_at: 2026-08-13, note: "SIX ROUNDS AGAINST A CAP OF 2, every round past the second scoped by Juano's ruling rather than granted by the orchestrator. 35 findings, 33 closed, 2 open by design (F-001, F-009 - both sdlc-implementer-backend's, both carried in TASK-002). Four security passes, all EXECUTED rather than read. Ten ADRs, three contracts, six stubs, all verified non-empty on disk. CONTRACTS ARE NOW FROZEN for this initiative. TASK-018 claims NO AC and that gap is knowingly approved - the role split postdates the plan's 36 criteria, the round-4 re-review judged it not load-bearing, and adding one would amend an approved STORY." }

  test:
    wave_0: { status: approved, approved_at: 2026-08-14, note: "TASK-018. 9 tests, 8 red as assertions, 1 green control that proves the harness can SEE a guard fire - without it the two guard failures would look identical whether the guard is broken or the harness cannot detect a RAISE EXCEPTION. test_exempt was available under test.md and Juano declined it." }
    wave_1: { status: approved, approved_at: 2026-08-14, note: "TASK-001 and TASK-002. 62 tests total across the wave, 14 red as assertions, 42 blocked on the six design stubs (F-046, accepted - the architect PROVED the correct red by copying the stubs in, recording the failures and deleting them again), 6 green on arrival and stated as such. No regression: the 62 existing tests all pass. BOTH SUITES RUN BY THE ORCHESTRATOR, not taken from an agent report. AC-8's typecheck clause is discharged by the gate command rather than an assertion, because ADR-0005 bans node:child_process in that package." }

escalations: []
# F-074 RAISED AND RULED 2026-08-14 (ADR-0051 rejects the compose default by value). Its own
# unpriced cost became F-081, which is open and needs an owner by wave 2.
# F-047 RAISED AND RESOLVED 2026-08-13/14. Out of this list rather than marked resolved in it,
# for the same reason F-044 was: check-ledger rule 3 reads the list itself, not a status on the
# entry. History in the log.
# F-044 RAISED AND RESOLVED 2026-08-13, both inside the Test phase. It is out of this list rather
# than marked resolved in it because check-ledger rule 3 compares the list against findings whose
# status is `escalated`, without reading a status field on the entry - so a resolved entry left
# here reads as a live escalation. The history is in the log below.
# F-002 and F-010 CLEARED 2026-08-13 by Juano's widen rulings.
# F-024 CLEARED 2026-08-13 by Juano's ruling to TAKE THE ROLE SPLIT; ADR-0050 designs it and
# ADR-0044 is now superseded_in_part_by ADR-0050 - its no-RLS decision stands, its refusal does
# not. No TASK is escalated.

# Four obligations from `foundation` land here, per .sdlc/roadmap.md's carried-forward table.
# They are inputs to this refinement, not findings against it.
carried_in:
  - F-018      # @Public() invitation routes have no IP-keyed limit in any environment today
  - F-300      # raw invite token in the URL path on BOTH the GET and POST accept legs
  - F-362      # ...and the two recorded fixes are not equivalent; a redirect covers only GET
  - F-386      # mail transport binding; ruled 2026-08-12, MAIL_TRANSPORT, unset -> NoopMailSender
  - F-401      # the mail STUB still binds NODE_ENV against the corrected contract
  - F-239      # ALTER DEFAULT PRIVILEGES grants shortkit_app DML on every new table
  - F-157      # no build-output scan covers dynamic routes, which is the authenticated surface
  - F-236      # the GDPR eraser erases nothing and reports success (owner deferred; watch item 4)

log:
  - "2026-08-12 refine: INITIATIVE OPENED. Scout dispatched for grounding before any question was put to Juano. TWO OF THE SCOUT'S CONCLUSIONS WERE WRONG AND I CHECKED THEM RATHER THAN REPEATING THEM: it reported `registerTenantScopedSurfaces() does not exist anywhere in the repo - could not confirm this premise`, and it is exported from apps/api/test/isolation/coverage.ts and called at registrations.ts:591; it also reported the CI gate as three jobs against index.md's four-check claim, which is a wording question about whether `gate` counts itself and is not load-bearing here. The scout searched src and the function lives under test/. ITS ABSENCE CLAIM WAS THE ONE THING IT FLAGGED AS UNCONFIRMED, WHICH IS WHY IT WAS CHEAP TO CATCH."
  - "2026-08-12 refine: GATE APPROVED. Five questions, stopped at ~95% rather than at the end of the list. THE CUT THAT MATTERED WAS THE FIRST ONE: invitations out, which took capability tokens, mail and four carried obligations with it and left a sentence that still stands alone. The other four followed from it - a workspace with one human in it has nothing to scope, so `memberships` is not built; verification cannot be required when MAIL_TRANSPORT unset binds NoopMailSender, so it is off as a DATED DECISION WITH ITS TRIGGER rather than as an omission; screens are in because an operator who signs up is a person at a screen and without them the increment's only user is a test; and SC-1 is inherited REWORDED, bounded to this initiative's surface, because the verbatim form is the shape F-295 refuses."
  - "2026-08-12 refine: THREE ACCEPTED ADRs ALREADY BIND THIS WORK and I read them rather than interviewing Juano about what the repo had settled. ADR-0013 fixes the auth mount, its middleware ordering and the two middlewares that are the fix for what mounting ahead of Nest costs. ADR-0015 fixes cardinality at UNIQUE (user_id) and SPLITS MEMBERSHIP ACROSS TWO TABLES AT TWO LEVELS, which is what makes the workspace-membership deferral clean - tenant_memberships is table stakes for signup and is IN, `memberships` is OUT, and they were never the same decision. ADR-0015 also states `Signup creates a tenant. Invited signup does not.`, so cutting invitations removes a branch rather than half a design."
  - "2026-08-12 plan: BOTH DISPATCHES DIED ON TRANSPORT ERRORS, NOT ON ANYTHING THEY FOUND. sdlc-planner ran 42 minutes across 13 tool reads and was cut mid-response with ~85k tokens spent; sdlc-vault-sync died the same way. I VERIFIED DISK RATHER THAN ASSUMING: repo tree clean, no new files under .sdlc/identity-membership, and notes.md is byte-identical to the heading I seeded at 12:56, so the sync never reached the one file it is forbidden to touch. Nothing to repair, nothing half-written."
  - "2026-08-12 plan: AND THE SHAPE THAT FAILED IS A WORKFLOW DEFECT, NOT BAD LUCK. sdlc-planner SHIPS READ-ONLY while phases/plan.md step 3 has the ORCHESTRATOR build the artifacts from its INLINE RETURN, so an entire EPIC/STORY/TASK tree - Interfaces blocks, coverage tables, Global Constraints copied verbatim - must transit as ONE VERY LARGE SINGLE RESPONSE. That is the response most likely to be cut mid-stream, and when it is cut THERE IS NO PARTIAL FILE TO RESUME FROM. Structurally identical to F-R, which I closed for the four auditors this same day: a phase file asking an agent for an artifact the agent has no way to persist. The F-A precedent applies unchanged - grant Write scoped to the planning artifacts and the failure costs one TASK card instead of the whole tree."
  - "2026-08-12 plan: RETRY SUCCEEDED, AND THE FIX I MADE FOR IT DID NOT TAKE EFFECT. The planner returned 24 cards and reported, unprompted: `I do not hold a Write tool - my tool set is Read and Bash only. Every card was written with Bash heredocs.` I HAD GRANTED IT Write MINUTES EARLIER. So agent definitions resolve at session start and my edit reaches the NEXT session, not this dispatch - WHICH MEANS TODAY'S F-R FIX FOR THE FOUR AUDITORS IS ALSO NOT LIVE YET. The heredoc workaround is the exact behaviour the foundation retro documented for the auditors. What saved this run was the DISPATCH PROSE telling it to persist each card before starting the next; the agent honoured the instruction with the tool it actually had and disclosed the substitution rather than letting me infer a Write had happened."
  - "2026-08-12 plan: VERIFIED THE CARDS MECHANICALLY BEFORE CITING ANY OF THEM, per invariant 9. 24 cards, front matter parses on every one, 36 ACs each claimed by EXACTLY ONE TASK, no duplicate ids, no empty paths, both owner slots valid, graph ACYCLIC across 22 edges with no edge to a nonexistent TASK. The planner also ran the reachability check as its own section and REPAIRED ONE INSTANCE BEFORE FINALISING - TASK-001's paths did not reach roles.ts, where it produces asTenantRole and tenantRoleRank. That is the F-S class caught inside the phase that creates it rather than nineteen times across an initiative, and the check cost one dispatch paragraph."
  - "2026-08-12 plan: STEP 7'S SECOND SIGNAL FIRES AND I AM NOT RESOLVING IT MYSELF. Wave 1 ships nothing - two zod modules and a migration, with app.module.ts still wiring only HealthModule - and phases/plan.md says that signal means STOP, present the split, and ask. THE HONEST READING IS THAT THE SIGNAL MISFIRES HERE: nine waves exist because four of them are single-TASK BY FILE SERIALISATION (auth.config.ts, main.ts, migration ordering, the contracts barrel), not because six increments are batched. And the split that would satisfy the rule - auth substrate first, screens second - REINSTATES THE `API PLUS TESTS ONLY` OPTION JUANO DECLINED AT THE REFINE GATE FOUR HOURS AGO. Stating the cost rather than deciding it."
  - "2026-08-12 plan: GATE APPROVED WITH THREE RULINGS, AND THE ONE JUANO DECLINED IS WORTH RECORDING AS DECLINED. scripts/** still reaches sdlc-implementer-backend only through config.yaml's `**` fallback. The planner raised it unprompted, naming the precedent - observability/** sat in no TASK's paths for six days while holding TASK-003's largest deliverable, and about forty findings routed correctly BY LUCK. Juano ruled the panel change in and this one out. IT IS NOW A KNOWN ACCIDENT RATHER THAN AN UNKNOWN ONE, which is a different risk: if a scripts/** finding misroutes during Implement, the record shows it was offered and declined, not missed."
  - "2026-08-12 plan: SC-2 CHANGED RATHER THAN THE PLAN, AND THE LIMIT IS ON THE CRITERION. `in a browser` came out; transport-level measurement discharges it. This is the third instance of Juano's pattern of clearing an unsatisfiable criterion BY CHANGING THE CRITERION AND REQUIRING THE RECORD TO SAY SO - A-8 on AC-6, F-154 on AC-113, now this. What goes unmeasured is named on SC-2 itself: rendering, client-side navigation, and anything that only breaks in a real browser engine. THE ALTERNATIVE WAS A FOURTH TEST TIER AND A TOOLING DECISION NOBODY HAD ASKED DESIGN FOR."
  - "2026-08-13 design: WAVE 1 ARTIFACTS WRITTEN AND VERIFIED ON DISK - six ADRs (0043-0048, numbering continued past foundation's frozen 0042), three contracts, six stubs plus a README, none empty. THE ARCHITECT DISCLOSED AN OUT-OF-SCOPE WRITE RATHER THAN HIDING IT: it amended .sdlc/foundation/design/contracts/tenant-context.md IN PLACE, outside its declared scope, because my dispatch said `that file is now yours`, and it offered to revert and re-issue as a separate file. git status confirms the modification. THE ALTERNATIVE IT REJECTED WAS A SPLIT ADDENDUM, on the grounds that a second copy is the drift this repo keeps filing findings about - which is F-250's reasoning applied unprompted."
  - "2026-08-13 design: AND IT CAUGHT THAT MY OWN DISPATCH REPEATED A STALE CLAIM. I told it check-policies.mts names four exempt tables and the jwt plugin adds a fifth; IT NAMES FIVE, `jwks` added by F-232 on 2026-08-07. THE ERROR WAS IN THE PLAN, WENT INTO TASK-002's CARD, AND I CARRIED IT INTO THE BRIEF WITHOUT CHECKING - three artifacts describing four where the code says five, two of them mine. I verified by reading the Map rather than the docblock, which is how I found the THIRD instance: the script's own comment says `None of the four exist yet`. Filed as F-001 against TASK-002 rather than fixed by me, per invariant 3."
  - "2026-08-13 design: STEPS 6 AND 8 HAVE NOT RUN. design.md requires a fresh-context disproving pass on the load-bearing ADRs and a design-mode security audit before this gate, and on a `full` track neither is optional. ADR-0045 and ADR-0046 are exactly what step 6 describes - correctness the type system cannot check - and ADR-0044 states a cost I will not carry to the gate unaudited: any SQL defect anywhere in apps/api reads every session.token, every account.password and every email in any tenant's request. NOT PRESENTING A GATE UNTIL BOTH HAVE RUN."
  - "2026-08-13 design: STEP 6 PAID FOR ITSELF ON ITS FIRST USE AND FOUND A BLOCKER BY BUILDING IT. The disproving reviewer created a scratch database, wrote the ADR's policy verbatim, and drove it through THE REAL pg.Pool with client.ts's own settings: mint #1 on a cold pool returned the tenant id, one ordinary tenant transaction ran, mints #2 and #3 raised 22P02 invalid input syntax for type uuid: \"\". THE MECHANISM IS THAT A TRANSACTION-LOCAL set_config LEAVES THE GUC AT THE EMPTY STRING, NOT NULL, and pg.Pool issues no reset - so the ADR's premise `with the flag unset current_setting returns NULL` is true only on a backend where the placeholder was never created. F-003. Reasoning would not have found this; the reviewer said so and measured instead."
  - "2026-08-13 design: AND IT IS NOT THE NEW POLICY'S DEFECT. F-005 - the reviewer rebuilt tenantScopedPolicies('links') with the SHIPPED redirectReadPolicy and got the identical raise on a warm connection, so THE PROPERTY BELONGS TO rls.ts:70, ALREADY MERGED, and ADR-0045 is the first caller to reach it. Nothing hits it today because links, domains and the eraser are all deferred - LATENT, NOT LIVE. It decides where the fix goes: repair only the new policy and the redirect escape stays armed, on the one path in this product that must never return 5xx to a visitor. THIRD APPEARANCE OF F-236's CLASS: a policy verified against the policy set and never against a statement issued under it, this time with CONNECTION STATE as the variable nobody varied."
  - "2026-08-13 design: THE LEDGER CHECK FIRED THREE FALSE POSITIVES AND THE SCRIPT WAS WRONG, NOT THE LEDGER. Rule 4 inferred `deferred` from absence in state.yaml's `tasks:` map - a map that only exists once an initiative reaches Implement - so every finding on a Design-phase initiative read as owned by a deferred TASK, against cards sitting at status: todo. ABSENCE OF EVIDENCE REPORTED AS EVIDENCE, which is the class the script exists to catch, in the script itself. It now reads deferral FROM THE CARD and falls back to the map only when no card exists. I PROVED THE RULE IS NOT A NO-OP RATHER THAN ACCEPTING A GREEN: a seeded deferred card with an uncarried blocker fires, a todo card does not, and adding the id to the roadmap clears it. My first regression attempt was INVALID - rule 4 is gated on roadmap.md existing and the probe had none - and I caught that by expecting a failure and getting clean."
  - "2026-08-13 design: ROUND 1 MADE THE FINDING WORSE, WHICH IS THE POINT OF A ROUND. The architect reproduced it independently BEFORE designing against it and found a case NEITHER F-003 NOR F-005 CONTAINED: an out-of-context count(*) on a tenantScopedPolicies table, warm connection, NO ESCAPE INVOLVED AT ALL, raises. That is AC-10's own shape, and rls.ts:52-56 says the `true` argument exists to make exactly that read return zero rows - THE COMMENT DESCRIBES THE BEHAVIOUR THE DEFECT REMOVES. So `which predicate carries the tolerance` was settled BY MEASUREMENT rather than by my preference or its own: the template. ADR-0049, nullif(<flag>, '') on every flag cast, amending ADR-0003 and ADR-0004 with both declared in frontmatter rather than absorbed."
  - "2026-08-13 design: AND IT DISPUTED F-005 WITH EVIDENCE INSTEAD OF ACCEPTING IT. redirectReadPolicy is a TEXT comparison and '' = 'on' is false, so it was never unsafe; the raise the reviewer saw came from the tenantScopedPolicies policy that `links` ALSO carries. Recorded as a `correction` on F-005 with the original text kept verbatim. THE CORRECTION MAKES THE FINDING STRONGER AND NARROWS THE REPAIR - the redirect path needs no change of its own. Two agents in one wave have now corrected a claim I passed them; the scout's absence claim was the other."
  - "2026-08-13 design: F-009 IS THE COST IT NAMED WITHOUT BEING ASKED. controls.ts:131 hand-writes the production predicate and THE DOCBLOCK ON THE NEXT LINE CLAIMS IT COMES `from the same production constant`. I verified both lines directly. After ADR-0049 the harness would go on proving isolation against the OLD predicate AND STAY GREEN, because the old form isolates correctly on a cold connection - the only state the fixture creates. F-288's class, found and not fixed, in the one file whose whole job is catching this. It also recorded the obvious AND-guard alternative as MEASURED WRONG: installed verbatim, re-run warm, still raises. That is the fix a reviewer accepts on sight."
  - "2026-08-13 design: TASK-002 WIDENED A SECOND TIME IN ONE WAVE, F-010, ruled by Juano. THE PATTERN IS WORTH THE RETRO'S ATTENTION: the plan's reachability pass checked every deliverable THE PLAN named and was clean; Design then named six more (F-002), and Design's own rework named two more again (F-010), because moving the fix into a shared template gave it more consumers than the policy it replaced. A reachability check is only ever as complete as the design it runs against, and Design runs after Plan. NOT AN ARGUMENT AGAINST THE CHECK - both widenings were caught before an implementer hit them, by the agent that created the need."
  - "2026-08-13 design: SECURITY PASS, ROUND 2 OF 2 - THE CAP. 1 blocker, 5 major, 2 minor, F-020..F-027, report at audits/design-sdlc-security-auditor-r1.md. IT EXECUTED RATHER THAN READ, in a scratch database it created and dropped. F-020: BETTER_AUTH_SECRET is bound NOWHERE across seven ADRs, three contracts and seventeen cards, and 1.6.26's validateSecret returns early under isTest() and throws on the default ONLY under isProduction - so the two environments that exist are the two it does not cover. I VERIFIED IT MYSELF at create-context.mjs:38-45 rather than taking the report. The default is the symmetric key for jwks.privateKey, so one jwks row plus a published constant is the signing key and every control in this initiative hangs off a forgeable tid. Dockerfile:83's unconditional NODE_ENV=production means compose WOULD refuse to boot; the exposure is local pnpm dev. AND THE SHAPE IS GC-B's OWN HAZARD ARRIVING FROM INSIDE A DEPENDENCY, where a rule this project wrote for itself cannot reach."
  - "2026-08-13 design: F-024 ESCALATED, AND IT REOPENS A REFUSAL RATHER THAN ADDING A FINDING. ADR-0044 priced its accepted cost IN READS - `reads every session.token, every account.password and every email` - and refused the shortkit_auth role split against that price. The auditor, from tenant A's ORDINARY transaction, OVERWROTE TENANT B's PASSWORD HASH, INSERTED A FORGED SESSION WITH A CHOSEN TOKEN AND CHANGED THEIR EMAIL, while the tenant-scoped read in the same transaction returned 0. F-239's ALTER DEFAULT PRIVILEGES is what grants the DML and no policy on the five exempt tables denies it. ACCOUNT TAKEOVER IS A STRICTLY LARGER CLAIM THAN THE ONE THE REFUSAL WAS MADE AGAINST. The ADR is not wrong to have written a cost down - it is wrong about the cost, which is a different and more dangerous thing, because the refusal LOOKS reasoned."
  - "2026-08-13 design: TWO FINDINGS ARE THIS INITIATIVE CONTRADICTING ITSELF INSIDE ONE ROUND. F-021: membershipLookupPolicy compares a TEXT flag and rests on `no user row has id ''` - and ADR-0049, written in the SAME round, names that exact hazard class in its own Consequences. The wave shipped the first instance of the hazard in the same commit that documented it. F-022: ADR-0049's cast control passes two renderings that still raise, so the enforcement offered IN PLACE OF documentation does not enforce. F-025: the ADR cites four controls, three `already existing`, and CONTEXT_FLAG_OWNERS HAS NO CONSUMER ANYWHERE - a narrowness argument resting on nothing. All three are the retro's `validity conditions nothing enforces` pattern, now inside the artifacts written to fix a different instance of it."
  - "2026-08-13 design: JUANO RULED F-024 - TAKE THE ROLE SPLIT, REVERSE ADR-0044's REFUSAL. The refusal was made against a read-only exposure and the measurement is account takeover, so the decision is re-made against the corrected cost rather than the original one. THE COST HE ACCEPTED IS THE ONE THE ORIGINAL REFUSAL WAS AVOIDING: reopening ADR-0031 and touching six provisioning artifacts across three waves. THE REASON IT WINS ANYWAY is that it is the only option where a SQL defect anywhere in apps/api cannot forge a session row, on a product whose entire claim is tenant isolation. The rejected alternative - keep the refusal, correct the prose - would have left the ADR honest and the exposure live."
  - "2026-08-13 design: AND HE AUTHORISED A THIRD ROUND, WHICH IS THE RULING design.md ASKS FOR RATHER THAN AN OVERRIDE OF IT. The file says the cap is 2 for `full` and that a design needing three has a problem rounds cannot fix - STOP AND TAKE IT TO JUANO. This is that stop, and the stop worked: the decision reached him instead of me spending a round on my own judgement. F-021 and F-022 are mechanical and already diagnosed with the fix named; F-020 and F-023 are `nothing decides this` rather than `this is wrong`, which is a decision to make and not a defect to repair. THE CAP DID ITS JOB BY FORCING THE CONVERSATION, NOT BY STOPPING THE WORK."
  - "2026-08-13 design: ROUND 3 CLOSED ALL EIGHT, AND FIVE OF THEM BY RE-EXECUTION RATHER THAN RE-READING. F-020..F-027 all ADDRESSED. THE ONE THAT MATTERED IS F-024: the auditor reproduced the takeover pre-split - UPDATE 1 / INSERT 0 1 / UPDATE 1 on another tenant's account, session and email from tenant A's ordinary transaction - and then got permission denied on all four tables from the IDENTICAL transaction after ADR-0050's REVOKE/GRANT. F-021 was proved causal in both directions with a ghost user.id = '' row; F-022's new counting control was run against 13 real pg_policies.qual renderings; F-023's log hook was executed against better-auth@1.6.26 for ZERO console fallbacks. A resolved_by field is a claim, and this is what checking one costs."
  - "2026-08-13 design: AND THE RE-REVIEW RAISED SIX OF ITS OWN, F-028..F-033, FOUR MAJOR. I VERIFIED FOUR OF THE SIX MYSELF BEFORE CARRYING THEM: 0000_odd_betty_ross.sql:33-34 does ship the raw current_setting form (F-029), rls.ts:137 is parameterless (F-030), ADR-0046 is still status: accepted with no amendment header (F-028), and git diff HEAD -- tasks/ IS EMPTY (F-032). THE LAST ONE CORRECTS MY OWN DISPATCH: I told the auditor TASK-002 and TASK-011 were revised this round because find -newermt said so. Their mtimes moved on a NO-OP WRITE and their content never changed. An mtime is not a diff, and I passed a false premise to an agent for the second time in this initiative - the first was the four-vs-five exempt tables that became F-001."
  - "2026-08-13 design: F-032 IS DESIGN.MD STEP 7 NOT HAVING RUN, WHICH IS MINE AND NOT THE ARCHITECT'S. Zero of seventeen cards mention BETTER_AUTH_SECRET, shortkit_auth, DATABASE_AUTH_URL, assertAuthRoleSeparation, CONTEXT_FLAG_OWNERS or any of ADR-0050/0051/0052. A BLOCKER FIX AND A REVERSED GATE DECISION WITH NO DELIVERY VEHICLE - the Implement phase runs from the card, and docker-compose.test.yml is in no card's paths at all. This is the observability/** precedent from foundation, where a deliverable sat in no TASK's paths for six days, except that here it is the fix for the blocker that reopened an ADR."
  - "2026-08-13 design: TWO OF THE SIX ARE THE ROLE SPLIT'S OWN CONTROLS FAILING THE WAY THE THING THEY REPLACE FAILED. F-031: assertAuthRoleSeparation checks SELECT on ONE table per direction, the attack it exists to close is an INSERT, and account and jwks are unchecked - measured, a REVOKE that omits INSERT on session leaves both boot directions green while tenant A forges a session for tenant B's user. THAT IS ADR-0044's EXACT ERROR - reasoning about the read half - REPRODUCED INSIDE THE ADR WRITTEN TO CORRECT IT. F-028: ADR-0046 still forbids the second pool ADR-0050 requires, and the cheap way out of the resulting sign-in failure is dropping the REVOKE, which silently restores the takeover."
  - "2026-08-13 design: F-029 WOULD TURN THE BUILD RED IN WAVE 1. ADR-0049's Decision says all four flags take the wrapper; its own migration and rls.ts sections fifty lines lower still say tenants_privileged_erase and redirectReadPolicy are untouched, and the new control rejects both untouched forms. The shipped 0000 migration already carries one, ADR-0004 is forward-only, and the ADR says not to write the 0001 statement that could fix it. THE PRESSURE AT THAT POINT IS ON THE CONTROL, NOT THE DDL - loosening a control to make a build green is the failure mode ADR-0049 was written to record."
  - "2026-08-13 design: STOPPING RATHER THAN TAKING A FOURTH ROUND ON MY OWN JUDGEMENT. Round 3 was ALREADY over design.md's cap of 2 and existed only because Juano authorised it. Its own re-review raising four majors is the signal the cap is written for. The cap's job last time was forcing the conversation rather than stopping the work, and the same applies: this goes to Juano as an adjudication, not as another round I grant myself."
  - "2026-08-13 design: FOUR RULINGS FROM JUANO ON THE ROUND-3 SURVIVORS. (1) NARROW ROUND 4 - the architect takes F-028, F-029 and F-031, the two that reopen F-024 plus the one that reds the wave-1 build; F-030 and F-033's ADR text rides along as one-sentence corrections because leaving a false claim in an accepted ADR is what F-025 was; F-032 IS MINE AND NOT THE ARCHITECT'S, because design.md step 7 is the orchestrator's step and TASK cards resolve to `orchestrator` under config.yaml's `.sdlc/**` rule. (2) assertBetterAuthSecretConfigured() MOVES FROM TASK-004 TO TASK-003, wave 3 to wave 2, so the config and its guard land together and no wave ships an auth surface booting on better-auth's published constant. (3) THE PLAN IS AMENDED rather than TASK-002 widened a third time: the unowned provisioning artifacts get an owning TASK and CI role provisioning moves into wave 1's reach."
  - "2026-08-13 design: (4) AND THE ORDERING FORK HIS RULING DID NOT SETTLE WENT BACK TO HIM RATHER THAN BEING DECIDED HERE. Provisioning CREATES shortkit_auth and TASK-002's migration 0001 REVOKEs on it, so the edge is hard - and a depends_on edge INSIDE a wave contradicts what a wave is, since wave membership is derived from the graph. Three shapes were possible and they differ in what they cost the record: WAVE 0, chosen, keeps waves 1-9 and every reference to them - design.wave_1 still means {001, 002}, the coverage table and the ledger are untouched. Renumbering to ten waves would have put every existing wave reference off by one INCLUDING THE GATE ALREADY RECORDED. Hiding the edge inside wave 1 satisfies invariant 11 on write surfaces and lies about the order."
  - "2026-08-13 design: AND READING THE REPO FOR THAT CARD FOUND THE RULING'S SCOPE IS WIDER THAN THE FINDING SAID. F-032 and ADR-0050 name three unowned artifacts; ROLE CREATION ACTUALLY LIVES IN THREE PLACES AND TWO OF THEM ARE NEITHER OF THOSE - docker-compose.yml:320 and docker-compose.test.yml:122 each carry their own inline CREATE ROLE block, alongside .github/scripts/provision-test-database.sql. So wave 0 has to reach docker-compose.yml, WHICH IS TASK-009's PATH IN WAVE 4. That is precedented rather than a collision - app.module.ts is touched by four TASKs across four waves - but it is a second claimant on a file the wave table has to separate, and it exists because the escalation enumerated the artifacts it could see from the ADR rather than from the repository. NOT WRITING THE CARD UNTIL ROUND 4 RETURNS: the architect is re-deriving this same provisioning story and I would write it twice."
  - "2026-08-13 design: ROUND 4 RETURNED AND THREE OF ITS FIXES ARE STRICTLY BIGGER THAN THE FINDING ASKED FOR, BY MEASUREMENT. F-031's required_change said assert the whole privilege set over the whole exempt list; the architect measured three facts that changed the shape beyond it - the comma list is ANY-of, has_table_privilege MISSES A COLUMN-LEVEL GRANT (GRANT SELECT (email) ON \"user\" leaves the table-level call false while SELECT email returns the row), and has_any_column_privilege REJECTS DELETE with unrecognized privilege type so its list is three not four. Both directions now read pg_class in ONE query because has_table_privilege on an absent table raises 42P01, with a separate migrations-have-not-run verdict. A finding written from reasoning, closed by execution, ending wider than it started."
  - "2026-08-13 design: AND IT CORRECTED ITS OWN ROUND-3 ARTIFACT UNPROMPTED. ADR-0051's follow-up said `the integration tier has no owner for the value`; auth-fixture.ts:85 ALREADY SETS a 53-character non-default BETTER_AUTH_SECRET. Struck and replaced, and what that fixture actually lacks - DATABASE_AUTH_URL - became a TASK-018 obligation. I VERIFIED IT ON DISK. Third agent in this initiative to correct a claim rather than carry it, after the scout's absence claim and round 1's dispute of F-005."
  - "2026-08-13 design: TWO OF THE CARD GAPS IT HANDED ME WERE FALSE AND I CHECKED THEM RATHER THAN APPLYING THEM. It reported TASK-002's paths still short of controls.ts and docs/architecture/rls.md - BOTH ARE THERE, added by the F-010 widening - and TASK-011 still quoting the pre-nullif predicate, WHICH F-011 CORRECTED IN ROUND 1. THE CAUSE IS MY DISPATCH: I wrote `do not touch tasks/**` meaning do not WRITE, and it read that as do not LOOK, so it inferred card state from ADR text. A boundary phrased as a prohibition on a path forbids reading it too unless it says otherwise, and an agent that cannot see a file will infer it rather than say it cannot see it. THIS IS THE SECOND FALSE PREMISE I HAVE PUT INTO A DISPATCH IN THIS INITIATIVE - the first was the mtime-not-a-diff error that became F-032. Round 4's re-review is dispatched with tasks/** explicitly readable."
  - "2026-08-13 design: F-032 REPAIRED AS THE ORCHESTRATOR'S OWN WORK, NOT ROUTED. design.md step 7 is the orchestrator's step and config.yaml routes .sdlc/** to `orchestrator`, so no implementer or architect was asked. TASK-002, -003, -004, -009 and -017 each gained a dated obligations section naming its ADR; TASK-018 created for wave 0; TASK-009's paths gained docker-compose.test.yml, WHICH WAS IN NO CARD'S PATHS AT ALL; plan.md gained the wave-0 row, the TASK-018 --> TASK-002 edge and four new multi-claimant file entries. main.ts IS NO LONGER TASK-004's SOLE FILE, which the wave table now says out loud."
  - "2026-08-13 design: TASK-018 CLAIMS NO AC AND THAT IS RECORDED AS A GAP RATHER THAN A DECISION. The role split arrived from F-024 after the plan's 36 ACs were written, so no criterion describes it. The honest alternatives were an AC added to STORY-003 - which amends an approved artifact and is Juano's call - or a marked config chore in TASK-010's shape. Took the second and flagged the first at the gate. NOT PAPERING IT OVER: every other TASK in this initiative claims at least one AC, and the check-ledger run that says `clean` does not test for this."
  - "2026-08-13 design: THE ROUND-4 RE-REVIEW CAUGHT ME MARKING MY OWN WORK FIXED WHEN IT WAS NOT. F-032 came back NOT ADDRESSED: four of six items done, two untouched - ADR-0045's set_config( grep control and ADR-0049's behavioural control still have no file, no owning card and no test_files entry. I verified by grep across all eighteen cards; neither string appears. AND THE FIRST ONE IS THE BASIS ON WHICH F-025 WAS CLOSED. F-025 was `an ADR cites four controls and none of them executes`, so a control with no file is that finding again, one round later, INSIDE ITS OWN FIX. Reopened with a correction block rather than left green. This is why the re-review is not optional even when the work is the orchestrator's - especially then, because nobody else was going to check it."
  - "2026-08-13 design: F-034 IS THE UNPRICED COST OF JUANO'S OWN F-033 RULING AND THE AUDITOR SAID SO IN THOSE TERMS. Moving the secret assertion into wave 2 closed a one-wave window and opened a two-wave one: assertBetterAuthSecretConfigured() is unconditional from wave 2 and DATABASE_AUTH_URL is needed from wave 2, while both variables reached the compose api service only in wave 4. VERIFIED MYSELF: ci.yml:382-386 fans `compose` into the required `gate`, and docker-compose.yml:227-228 gives api exactly one variable. So waves 2 and 3 could not go green. THE REDNESS IS NOT THE DANGER - THE REPAIR PRESSURE IS: the two cheapest fixes are deleting the assertion Juano just moved, or giving DATABASE_AUTH_URL a fallback to DATABASE_URL, WHICH ADR-0050 NAMES BY NAME as the change that silently restores shortkit_app as the auth role with every gate green."
  - "2026-08-13 design: AND THE FIX UNDOES NOTHING, WHICH IS WHY IT WAS WORTH FINDING RATHER THAN LIVING WITH. Four lines into a card that already holds both compose files. Juano ruled the declarations into TASK-018/wave 0; TASK-009 keeps .env.example and README. THE COST HE ACCEPTED IS THAT WAVE 0 IS NO LONGER `PROVISIONING ONLY` - it is now everything the auth surface needs to exist before it is built, which is a slightly worse name for a slightly better wave."
  - "2026-08-13 design: ROUND 5 AUTHORISED, AND ITS BRIEF IS THREE SENTENCES OF ADR TEXT. Cap is 2, this is the fifth, and the reason it is not a cap failure is that rounds 4 and 5 were both scoped by ruling rather than granted by me: round 4 took three findings, round 5 takes two ADR sentences and one factual check. EVERYTHING ELSE IN F-034..F-038 IS CARD AND PLAN TEXT, WHICH IS ORCHESTRATOR WORK AND COSTS NO ROUND AT ALL - F-035 both directions of the grant matrix, F-036 apps/web/.env.example reads neither variable, F-037 the contaminant guard's fourth variable, F-038 the stale counts. Five findings closed without a dispatch."
  - "2026-08-13 design: THE AC GAP IS KEPT ON REASONING RATHER THAN ON THE PRECEDENT I CITED FOR IT, BECAUSE THE PRECEDENT WAS WRONG. TASK-018's card claimed TASK-010's shape as the model for a chore with no AC; TASK-010 CLAIMS AC-36, and TASK-018 is the only card of eighteen with an empty list. The re-review caught it. It also judged the gap not load-bearing on its own terms - a skipped TASK-018 fails loudly one wave later at the migration's GRANT, and the widened CI guard plus assertAuthRoleSeparation catch the state it would leave. Corrected in place: kept, but for the actual reason. I HAD REACHED FOR A PRECEDENT WITHOUT CHECKING IT, WHICH IS THE THIRD TIME IN THIS INITIATIVE - after the four-vs-five exempt tables and the mtime-not-a-diff."
  - "2026-08-13 design: ROUND 5 CLOSED F-032's REMAINDER AND THE FILE LOCATION IS THE WHOLE ANSWER. ADR-0045's grep control is apps/api/src/db/context-flag-owners.spec.ts and it MUST live under src/** - vitest.config.ts:10 includes src/**/*.spec.ts AND NOTHING ELSE, so the same file under test/ would collect in no tier and PASS BY NOT RUNNING. That is the F-025 failure mode reproduced by filing a control in the wrong directory: it would exist, it would be cited, and it would never execute. ADR-0049's behavioural control is apps/api/test/tenancy/warm-connection-no-context.int-spec.ts, and apps/api/test/tenancy/ WAS IN NO CARD'S PATHS - verified across all eighteen."
  - "2026-08-13 design: AND THE ARCHITECT WENT BEYOND ITS BRIEF TWICE, FLAGGED BOTH, AND ONE OF THEM WAS LOAD-BEARING. ADR-0049's behavioural control said `for every table in schema public`; as shortkit_app after migration 0001 THE FIVE EXEMPT TABLES ANSWER permission denied (42501), NOT ZERO ROWS - so the control as written FAILS ON THE DAY ADR-0050 LANDS. Replaced with a computed has_table_privilege set, which has a bonus property it recorded: a sixth auth table nobody revoked stays in the set, has no policy, returns rows, and the control fires. It offered to have this reverted and re-filed as a finding rather than assuming the latitude. THAT IS THE SECOND TIME IN THIS INITIATIVE A CONTROL WAS FOUND TO BE WRITTEN AGAINST A STATE THAT NO LONGER EXISTS, after F-009."
  - "2026-08-13 design: AND IT CAUGHT AN ERROR IN MY OWN CARD TEXT WITHIN AN HOUR OF MY WRITING IT. I put BETTER_AUTH_SECRET and DATABASE_AUTH_URL on `the api service in BOTH compose files`; docker-compose.test.yml HAS NO api SERVICE - it runs postgres alone, verified. The integration tier gets both through auth-fixture.ts, which TASK-018 already owns. FOURTH UNCHECKED CLAIM OF MINE THIS INITIATIVE - after four-vs-five exempt tables, mtime-not-a-diff, and TASK-010's non-existent AC precedent. Every one was caught by an agent reading the repository rather than by me re-reading my own text, WHICH IS AN ARGUMENT ABOUT WHERE TO SPEND VERIFICATION, NOT ABOUT BEING MORE CAREFUL."
  - "2026-08-13 design: FIVE FINDINGS CLOSED WITH NO DISPATCH AT ALL - F-034 through F-038 are card and plan text, which config.yaml routes to `orchestrator`. Worth recording against the round count: this wave shows 5 rounds against a cap of 2, and that number OVERSTATES the design churn. Rounds 4 and 5 were scoped by Juano's ruling rather than granted by me, round 5 was three ADR sentences, and the largest single body of round-4/5 work - eighteen TASK cards and the plan - cost no round because it was never the architect's to do."
  - "2026-08-13 design: ROUND 6 CLOSED THE LAST TWO AND FOUND THAT MY OWN F-039 REPLACEMENT WORDING WAS ITSELF RED ON ARRIVAL. I specified the control as `every set_config( first argument in apps/api/src appears in CONTEXT_FLAG_OWNERS`. tenant-context.ts:217-219 SETS statement_timeout AND idle_in_transaction_session_timeout THROUGH set_config and neither is a registry row - VERIFIED BY GREP, exactly three distinct first arguments exist in apps/api/src and two are these. So the fix for a control that fails on first run was a control that fails on first run. It now filters on the `app.` prefix and matches on the { flag, file } PAIR rather than the flag alone, pair-level being what catches a second file setting an already-registered flag - which is clause A1's actual point. THE DEFERRED EQUALITY FLIP INHERITS THE FIX, so the same defect does not wait in TASK-054."
  - "2026-08-13 design: THAT IS THE FIFTH UNCHECKED CLAIM OF MINE THIS INITIATIVE AND THE PATTERN IN ALL FIVE IS THE SAME. Four-vs-five exempt tables, mtime-not-a-diff, TASK-010's non-existent AC precedent, the api service in a compose file that has none, and now a grep predicate written from the shape of the argument rather than from the file. EVERY ONE WAS A CLAIM ABOUT A FILE I HAD NOT OPENED AT THE MOMENT I WROTE IT, and every one was caught by an agent that did open it. The lesson is not `be more careful` - it is that a claim about a file's contents is worth one grep BEFORE it goes into an artifact, because after that it costs a round."
  - "2026-08-13 design: WAVE 1 DESIGN COMPLETE AND PRESENTED. 10 ADRs, 3 contracts, 6 stubs plus a README, all verified non-empty on disk. 35 findings filed, 33 closed, 2 open - F-001 and F-009, both sdlc-implementer-backend's and both carried in TASK-002's paths and Approach. SIX ROUNDS AGAINST A CAP OF 2, and every round past the second was scoped by Juano's ruling rather than granted by me. EIGHT FINDINGS CLOSED WITH NO DISPATCH AT ALL because card and plan text routes to `orchestrator`. The security auditor executed rather than read in all four of its passes; the single measurement that reversed ADR-0044's refusal is upstream of three of the ten ADRs."
  - "2026-08-13 test: PHASE HELD AT STEP 1 ON AN ESCALATION I RAISED AGAINST MY OWN DESIGN. F-044: ADR-0045's wave-1 grep control re-solves what isolation-coverage.md's clauses A1..A4 ALREADY SOLVE - same subject, every set_config( first argument and which file may set which flag - and A4 carries a CLOSED PERMITTED LIST naming statement_timeout and idle_in_transaction_session_timeout. Round 6 rediscovered that same hazard from scratch two hours earlier and answered it with an `app.` prefix filter. TWO MECHANISMS, TWO PERMITTED LISTS, NEITHER AWARE OF THE OTHER, and the contract's is the one a reviewer finds first. isolation-coverage.md is FROZEN and is in TASK-002's own contracts: list."
  - "2026-08-13 test: AND THE SCOUT'S FLAG THAT LED ME THERE WAS HALF WRONG, WHICH IS WHY READING THE CONTRACT MATTERED. It reported the grep control as a departure from house practice, citing logging-opt-out.spec.ts's TypeScript-AST parse and writing-good-tests.md's warning against grepping source. THE CONTRACT MANDATES TEXT SCANNING DELIBERATELY AND SAYS SO: none of the four clauses parses TypeScript or distinguishes code from a comment, because a commented-out setter is one uncomment from real and A2 is BUILT to fire on it. I repeated the scout's framing to Juano before checking it. The check cost one file read and turned a style question into F-044."
  - "2026-08-13 test: JUANO RULED RESTATE ADR-0045 AGAINST A1/A4 rather than amend the contract or ship both. The ADR's control becomes the wave-1-runnable subset half of clauses A1 and A4, cites them, and INHERITS A4's PERMITTED LIST instead of its own filter; TASK-056 later flips it to A1's exactly-one form rather than replacing it. This reinterprets the frozen contract without amending it, SO THE DESIGN GATE DOES NOT REOPEN - the option that would have reopened it was carrying both in the contract, and the option that would have left the ADR and the card disagreeing was shipping both. TASK-002's card written first; the ADR edit is QUEUED BEHIND THE VAULT SYNC rather than dispatched, because the sync reads design/** and a reader and a writer on one file is invariant 11's own rule."
  - "2026-08-13 test: test_exempt DECLINED FOR TASK-018. test.md would have allowed it - no AC, and its own card calls it a marked config chore - and Juano ruled real tests instead, on the grounds that the wave-0 role split is what the F-024 ruling bought and an exemption leaves it with nothing asserting it landed until TASK-002's grant matrix runs a wave later. Four things to pin, all of them failures this repository has actually had: three roles come up; shortkit_auth holds LOGIN/NOBYPASSRLS/no-superuser/no-ownership; the widened BYPASSRLS guard FIRES on a BYPASSRLS auth role; the widened cardinality guard fires on a two-role database. THE THIRD IS THE DIFFERENCE BETWEEN A GUARD AND A GUARD-SHAPED COMMENT."
  - "2026-08-13 test: SCOUT REPORTED THE FIXTURE SECRET AS 55 CHARACTERS AND IT IS 53. Measured with len(), not counted by eye. No consequence - both clear ADR-0051's 32-char floor and the card already said 53 - but it would have gone into the test dispatch as a fact, and this initiative's last five defects all entered exactly that way. THE SCOUT WAS RIGHT ABOUT EVERYTHING LOAD-BEARING: the tier suffixes, assertEveryIntegrationSpecRuns() catching an off-suffix integration file at config-eval time, CONTEXT_FLAG_OWNERS having zero importers, and authServerEnv() not yet returning DATABASE_AUTH_URL."
  - "2026-08-13 test: F-044 CLOSED IN ROUND 7 AND THE TEST PHASE IS UNBLOCKED. ADR-0045's control is now clauses A1 and A4 of isolation-coverage.md run over the wave-1 scan set, citing :487-501, :534-535, :540-542 and :527-532; the `app.` prefix filter is struck in place and deleted; TASK-056 is named as the flip to exactly-one, gated on TASK-029 and TASK-054. VERIFIED ON DISK - ten citations of the contract, the filter struck, and the TASK-056 follow-up carrying the obligation WITH ITS OWN REASON FOR EXISTING, because TASK-056 is deferred out of this initiative and that bullet is the only place the obligation can live. The contract was NOT amended, so the Design gate does not reopen."
  - "2026-08-13 test: AND THE ARCHITECT CAUGHT MY CARD CONTRADICTING ITSELF IN NINE LINES. TASK-002's lead sentence still instructed the `app.` prefix filter that its own sub-bullet, nine lines later, forbade - an implementer reading top-down writes exactly what the ruling deleted. Second: line 192 named the equality flip's owner as `whichever TASK lands the second setter`, WHICH IS THE ENABLING CONDITION AND NOT THE OWNER; it is TASK-056's. Both real, both fixed with dated notes. I told it to report disagreements with my card rather than resolve them silently, and that instruction is what produced these."
  - "2026-08-13 test: THAT IS A SIXTH SLIP OF MINE AND A DIFFERENT KIND FROM THE OTHER FIVE, WHICH MATTERS FOR THE RETRO. The first five were claims about files I had not opened - exempt-table count, mtime-as-diff, TASK-010's AC precedent, an api service in a compose file that has none, a grep predicate written from the shape of the argument. THIS ONE IS AN EDIT LAYERED ONTO MY OWN TEXT WITHOUT RE-READING WHAT IT NOW CONTRADICTS. The fix for the first five is one grep before the claim goes in; the fix for this one is reading the whole block after editing part of it. Same symptom, different cause, and a retro that merges them will propose the wrong control."
  - "2026-08-13 test: RESOLVED ESCALATIONS COME OUT OF state.escalations RATHER THAN GETTING A status: resolved. I marked F-044 resolved in place and check-ledger fired: rule 3 compares the list against findings whose status is `escalated` via e.findings, WITHOUT READING ANY STATUS FIELD ON THE ENTRY, so a resolved entry left in the list reads as a live escalation. Second time today the script has corrected my use of it - the first was filing an escalation without registering it at all, and before that the entry shape (`id:` where it wants `findings:`). THE SCRIPT IS DOING THE JOB IT WAS WRITTEN FOR after a run where ten ledger fields lagged the prose describing them."
  - "2026-08-14 test: GATE APPROVED FOR WAVES 0 AND 1, AND THE DESIGN GATE RE-APPROVED FOR ONE AMENDMENT. 62 tests across nine files: 14 red as assertions, 42 blocked on the six design stubs, 6 green on arrival and stated as such rather than counted as coverage. No regression - the 62 existing tests all pass. I RAN BOTH SUITES MYSELF rather than taking either architect's numbers, and the integration run took four minutes because the isolation harness alone is 188 seconds."
  - "2026-08-14 test: THE TWO CONTROLS THE DESIGN PHASE SPENT THREE ROUNDS ARGUING OVER BOTH PAID OFF ON FIRST RUN, which is the strongest evidence this initiative has produced that the argument was worth having. The warm-connection control reproduced ADR-0049's row 4 EXACTLY - 22P02 on both readable tables - with a cold-backend control green beside it proving connection state is the variable and not the query. The A1-subset control, the one F-044 was spent making cite the contract instead of duplicating it, FOUND ON ITS FIRST RUN that wave 1's design introduces a fourth context flag the frozen contract did not carry. A control left as its own mechanism would have passed."
  - "2026-08-14 test: F-047 AMENDED A CONTRACT THAT HAD BEEN FROZEN FOR TEN DAYS AND THE AMENDMENT IS BETTER THAN THE BRIEF I WROTE FOR IT. I asked for a table row and a widened carve-out. The architect also verified BY EXECUTION that A4's predicate needs no edit - its app. branch is a prefix match, run against the literal rather than read - and SEPARATED TWO LISTS I HAD CONFLATED: ISOLATION_EXCLUSIONS goes to three, not four, because tenant-context.ts is the ordinary request path and was never an exclusion. My worry that the stub's `EXCLUSION 3 OF EXACTLY 3` was about to break was wrong, and the reason it was wrong is worth keeping."
  - "2026-08-14 test: AND IT DECLINED TO FIX SOMETHING IT FOUND, WITH A REASON. A sixth stale `exactly two` site that ADR-0045 never enumerated - this contract's own length assertion. It left it alone, arguing a length-assertion bump belongs in TASK-002's commit beside the entry that raises it rather than in an amendment written a wave early. That is the judgement F-032 wanted and did not get: an obligation placed where it will be executed, not where it was noticed."
  - "2026-08-14 test: FIVE AGENT CORRECTIONS OF MY OWN CLAIMS THIS PHASE, WHICH IS MORE THAN ANY EARLIER PHASE PRODUCED. The test architect named the unique-constraint test AC-2 where my brief said AC-4, and found AC-4's mint half is not dischargeable in TASK-002 at all - AC-4 IS NOW CLAIMED BY TWO TASKS, breaking plan.md's `36 ACs each claimed by exactly one TASK` for the second time this initiative, recorded rather than resolved by re-cutting the AC. The architect corrected my reading of the Error-cases row as `ambiguous` when it was literally wrong. It corrected my ISOLATION_EXCLUSIONS worry. And it caught TASK-002's card contradicting itself in nine lines. EVERY ONE CAME FROM AN AGENT READING A FILE I HAD REASONED ABOUT."
  - "2026-08-14 implement: TASK-018 DONE. Wave 0 complete. Four fix rounds, 24 findings raised against one provisioning TASK, 22 closed and 2 routed out. Full integration suite: 72 passing, up from 65 - the eight red provisioning assertions went green and NOTHING ELSE MOVED. The three remaining reds are wave 1's own work: the AC-12 exclusion count, ADR-0049's warm-connection control, and the stub load error."
  - "2026-08-14 implement: THE PANEL'S VALUE WAS CONVERGENCE, NOT COVERAGE. THREE AUDITORS INDEPENDENTLY FOUND THE SAME MISSING LINE in docker-compose.test.yml's header, by three different routes - the reviewer from ADR-0050's assignment, the product auditor from the card naming it twice, the security auditor from what a developer does when the header is silent. ONLY THE THIRD FRAMING MADE IT A BLOCKER: the cheapest repair from that silence is DATABASE_AUTH_URL=\"$DATABASE_URL\", which ADR-0050 names as silently restoring shortkit_app as the auth role with every gate green. Three also found the contaminant-guard gap already filed as F-037, which is the evidence that writing it onto TASK-017's card was worth doing."
  - "2026-08-14 implement: I VERIFIED GREEN AND FOUND RED, AND THE IMPLEMENTER WAS NOT WRONG. It reported nine tests green; two failed for me. Cause: it had torn the docker-compose.test.yml stack down during its own verification and never restored it. Its claim was true when made and false when I checked, because SHARED MUTABLE STATE WAS LEFT IN A DIFFERENT CONDITION THAN IT WAS FOUND. Invariant 11 names exactly this - one database, one Docker daemon, each serialising the agents that touch them - and the cost was a false red that looked like broken code. Every later dispatch carried an explicit leave-it-up instruction and every later round complied."
  - "2026-08-14 implement: FIVE OF MY OWN CLAIMS WERE CAUGHT THIS TASK, AND TWO OF THEM I HAD ALREADY WRITTEN INTO ARTIFACTS. (1) I counted eight files in a fix diff that had seven - the eighth was findings.yaml, MY OWN LEDGER EDIT, counted as the implementer's. (2) I called apps/api/.env.example a phantom path; it is TASK-009's wave-4 deliverable, and TASK-009's own prose says so - I inferred from the file's absence without reading the card that creates it, AND PUT THE ERROR IN THREE CARDS. (3) I then pasted the correction into three cards where two have no such path entry to defend. (4) I told the implementer pg_hba behaves differently 'over loopback' when the true distinction is INSIDE THE CONTAINER - it compressed my three dropped words into a note that sends a developer looking for an error they cannot see. (5) I judged an Error-cases row 'ambiguous' when it was literally wrong."
  - "2026-08-14 implement: AND THE PATTERN ACROSS ALL FIVE IS NARROWER THAN `BE MORE CAREFUL`. Every one was a claim about a file's contents made without opening the file at the moment of writing, or a claim about my own edit made without re-reading it. The two that reached artifacts are the expensive ones, because a correction that is confidently wrong is worse than the vague thing it replaced - which is F-009's, F-030's, F-050's, F-059's and F-072's class, filed five times in this initiative and then committed by me twice."
  - "2026-08-14 implement: THREE FINDINGS LEAVE WAVE 0 UNOWNED AND THEY ARE THE SAME DEFECT. F-081 the compose secret-generation step, F-083 the rotation template's wave-2 story, F-084 security-headers.int-spec.ts's two-DSN message. EIGHTEEN CARDS AND NONE OWNS ci.yml, docs/architecture/migrations.md, the root .env.example OR security-headers.int-spec.ts - the first three were routed by ruling during this TASK and the fourth is still open. The plan's path declarations are not a partition of the repository, and every wave so far has discovered that the same way: an auditor finds a file nobody owns."
