slug: launch-core
title: Launchable core — tenancy, links, redirect, custom domains
type: feature
phase: test
created: 2026-08-03
updated: 2026-08-03

gates:
  refine:    { status: approved, approved_at: 2026-08-03 }
  plan:      { status: approved, approved_at: 2026-08-03 }
  design:    { status: approved, approved_at: 2026-08-04 }
  test:      { status: pending, approved_at: null }
  implement: { status: pending, approved_at: null }
  ship:      { status: pending, approved_at: null }

tasks:
  TASK-001: { status: rework, test_exempt: true, commits: "f58f340", audit_round: 1, rework_count: 0, open_findings: [F-042, F-043, F-044, F-045, F-046, F-047, F-048, F-051, F-052, F-053, F-055] }
escalations:
  - { phase: test, task: TASK-001, findings: [F-042], reason: "MAJOR. Contradicts ADR-0005's alternatives table, which rules out the dist-build option on typecheck grounds only and never addresses the API's runtime. implement.md: a finding that contradicts an approved contract is Juano's call, not a fix." }
  - { phase: test, findings: [F-050, F-053, F-055], reason: "land on approved artifacts (STORY-005 AC mint, TASK-001 paths, ADR-0018's pin stance); routing rule 0 forbids routing them to an agent" }

log:
  - "2026-08-03 refine: initiative opened"
  - "2026-08-03 refine: sdlc-scout dispatch skipped — repo contains only the scaffold commit (git ls-files: 5 files, all written this session). No prior art, conventions, or ADRs exist to ground against."
  - "2026-08-03 refine: refinement.md drafted; 8 success criteria, 6 additions accepted into scope, 2 deferred (password-protected links, bulk CSV import)"
  - "2026-08-03 refine: stop-slop pass applied to Problem, Outcome, Scope, Constraints, Risks"
  - "2026-08-03 refine: GATE APPROVED by Juano. Phase advanced to plan."
  - "2026-08-03 plan: sdlc-planner dispatched; returned 61 TASKs / 6 EPICs / 22 STORIEs. Breakdown at work/plan-breakdown.md"
  - "2026-08-03 plan: 3 STORIEs failed DoR (007 roles undefined, 019 SC-6 vs GDPR erasure conflict, 022 post venue undefined). Escalated to Juano per routing rule 0 (findings against approved artifacts never route to an agent)."
  - "2026-08-03 plan: Juano ruled. A-1 roles owner/admin + workspace_admin/member/viewer. A-2 append-only scoped to tenant-facing API, deletion is a privileged path. A-3 SC-8 + STORY-022 + TASK-058..061 deferred out of launch-core. A-4 optional per-workspace fallback URL confirmed. Amendments recorded in refinement.md."
  - "2026-08-03 plan: planner resumed via SendMessage to apply rulings and recompute edges, waves, coverage, DoR."
  - "2026-08-03 plan: revision applied. All DoR failures cleared; no STORY FAILs. Corrected: only 3 TASKs (042,043,044) are apex-domain-blocked, not 10 — STORY-016 and TASK-038..041 are not blocked."
  - "2026-08-03 plan: adopted the alternative sequence as default — EPIC-005 runs before the 3 domain TASKs, which move to trailing wave D. Costs nothing if the domain is registered early."
  - "2026-08-03 plan: config.yaml machine-validated for the first time via `npx js-yaml` (exit 0). Nothing installed. Constraint met."
  - "2026-08-03 plan: tech-writing initiative stub created as SC-8's home."
  - "2026-08-03 plan: 84 artifact files written (6 epics, 21 stories, 57 tasks) + plan.md. Cross-references verified: every TASK->STORY and STORY->EPIC resolves; all 57 TASKs claimed by a STORY."
  - "2026-08-03 plan: GATE APPROVED by Juano. Ids frozen. Phase advanced to design."
  - "2026-08-03 design: sdlc-scout skipped again — repo still has no source (TASK-001 unrun). Documented, not silent."
  - "2026-08-03 design: sdlc-architect dispatched as ONE cluster per phases/design.md step 3. Returned 20 ADRs, 18 contracts, 24 stub files. Verified on disk, none empty, no NUL corruption."
  - "2026-08-03 design: Better Auth spike risk CLOSED by ADR-0013 — toNodeHandler mounted directly on Express ahead of Nest; community NestJS wrapper rejected. No spike needed."
  - "2026-08-03 design: architect escalated 6 items. Juano ruled on 3. A-5 SC-2 reading fixed (500 RPS is the measurement rate; PR gate 100 RPS median-of-3; full run weekly/on-demand). A-6 one tenant per user, resolving the AC-22 vs AC-33 contradiction. A-7 click-event loss window accepted (<=1s / 100 events on ungraceful shutdown)."
  - "2026-08-03 design: 3 defects in approved artifacts corrected — TASK-007 paths gained apps/api/src/common/errors/**; TASK-013 Produces gained tenant_memberships with UNIQUE(user_id); config.yaml ownership comment corrected (ADR-0005: direct source import, no generated client)."
  - "2026-08-03 design: contracts: injected into all 57 TASK front-matter blocks. 53 bound to >=1 contract; TASK-001..004 correctly have none (infrastructure, no interface boundary). Every referenced contract path verified to exist."
  - "2026-08-03 design: stub ordering gap recorded — phases/design.md step 5 wants compiling stubs, but no workspace exists until TASK-001. Stubs written as sources under design/stubs/; TASK-001 and TASK-007 must materialise and verify them. Candidate workflow finding alongside F-M."
  - "2026-08-03 design: sdlc-security-auditor dispatched in design mode over ADRs + contracts. Awaiting verdict."
  - "2026-08-03 design: security audit r1 verdict CHANGES-REQUESTED. 3 blockers, 10 majors, 3 minors, 1 nit. Recorded as F-001..F-017 in findings.yaml; full audit at audits/design-sdlc-security-auditor-r1.md."
  - "2026-08-03 design: blockers were F-001 public invitation routes have no sanctioned tenant context (would force a permissive policy allowing unauthenticated cross-tenant enumeration); F-002 GDPR erasure denied by its own policy set, deletes nothing while reporting success; F-003 no domain-state gate and no reserved-hostname list, so any user can claim Shortkit's own hostname."
  - "2026-08-03 design: audit CONFIRMED the two GC-5 exclusions hold under direct attack. Click emission, the domain reconciler and the branding port are correctly not escapes. What broke was the accounting, not the escapes: F-005 found DELETE FROM tenants cascading with row security bypassed while setting no context flag, which the grep assertion cannot see."
  - "2026-08-03 design: F-012 escalated to Juano (bounded by Amendment A-1's frozen tenant enum). RULED as Amendment A-8: TenantRole = owner|admin|member, member at rank 0 granting nothing; invitees receive member."
  - "2026-08-03 design: F-014 minor treated as blocking per routing.md severity policy — it sits in a frozen normative block copied verbatim by TASK-011."
  - "2026-08-03 design: rework round 1/5 dispatched — same architect resumed via SendMessage with all 17 findings bundled into one dispatch per routing.md."
  - "2026-08-04 design: rework round 1/5 complete. Architect addressed all 17; none disputed as a finding, one prescribed MECHANISM corrected (F-004: express.json would consume the stream Better Auth needs; substituted a non-parsing byte cap). 5 ADRs changed decision, not just wording. Artifacts now 22 ADRs / 20 contracts / 27 stubs."
  - "2026-08-04 design: scoped re-review r2 verdicted ALL 17 round-1 findings ADDRESSED at source, and withdrew its own prescribed express.json mechanism after checking the substitute against all four cases it filed."
  - "2026-08-04 design: re-review found 4 NEW majors introduced by the fixes (F-018..F-021) plus 2 minors. Pattern in three of four: a fix created a surface and the artifact documenting it does not say who guards it. F-020 is the sharpest — the cheapest green fix would let any tenant member erase the whole tenant."
  - "2026-08-04 design: Juano ruled on the A-8 role-enum collision — brand TenantRole and WorkspaceRole as nominal types. Supersedes F-023 in a stronger form. Cheap now (stubs and prose), expensive after Implement."
  - "2026-08-04 design: rework round 2/5 dispatched with F-018..F-023 plus the branding ruling and 5 nits bundled for free."
  - "2026-08-04 design: rework round 2/5 complete. All 6 addressed. F-019 solved with a THIRD mechanism neither offered: the email-keyed limit moved inside Better Auth as a hooks.before middleware, where the body is already parsed by the framework that owns it. No buffering, no dropped bucket."
  - "2026-08-04 design: ADR-0023 added — role types nominally branded per Juano's ruling, plus the auditor's narrower AuthorisingTenantRole so 'member' cannot be a minimum by type. 23 ADRs / 20 contracts / 27 stubs."
  - "2026-08-04 design: architect flagged that isolation-coverage.md now carries an 'unenumerable' section — the Better Auth hook and in-handler authorization are covered by named integration tests, not structural enumeration. Narrows what SC-1/AC-93 'enumerates every surface' means. Must be stated at the gate and in any post citing SC-1."
  - "2026-08-04 design: architect could not verify Better Auth's hooks.before API shape against a running install; F-019's fix depends on it. Fallback is buffer-and-re-emit, to be revisited rather than improvised by an implementer."
  - "2026-08-04 design: scoped re-review round 3 dispatched over the 17-file round-2 revision."
  - "2026-08-04 design: scoped re-review r3 verdicted ALL of F-018..F-023 ADDRESSED at source, including that asTenantRole cannot launder 'member' into AuthorisingTenantRole, and that F-019's seam ordering is sound."
  - "2026-08-04 design: 4 new findings F-024..F-027, all clustered on F-019's replacement — the email bucket is well-designed and unowned. Hook lives in TASK-009's paths while TASK-051 is named producer and cannot write that file; TASK-009 runs before TASK-030 produces the redisClient; key unnormalised; no test would notice if the bucket never fired (silent fail-open)."
  - "2026-08-04 design: F-026 escalated under routing rule 0 (approved artifact) and RULED by Juano. TASK-051 Approach amended by the orchestrator: RateLimitGuard covers every route under /api — tenant-keyed when authenticated, IP-keyed on all methods including GET when @Public(). The rate-limit.md Boundary-line half routed to the architect."
  - "2026-08-04 design: rework round 3/5 dispatched (same architect — rounds 1-3 resume). F-024, F-025, F-027 plus 6 deferred observations bundled, including a TS4023 declaration-emit risk in the branding edit."
  - "2026-08-04 PROCESS DEFECT: the architect's round-1 and round-2 returns were never persisted to .sdlc/ and their task output file has since been cleaned up, so they are unrecoverable. The decisions survive in the ADRs and this log; the rationale layer does not. The security auditor independently hit this — it could not read the design return and had to mark two findings as carrying an unresolved dependency. Retro item alongside F-M (greenfield init) and the greenfield-stubs ordering gap. Fix: persist every subagent return to work/ on arrival, not at commit time."
  - "2026-08-04 design: rework round 3/5 complete. F-024, F-025, F-027 addressed plus the rate-limit.md half of F-026 and all six bundled observations. Architect confirmed its round-2 return had over-claimed an AC-84 scoping sentence it never wrote, and found the same declaration-emit defect in its own branding fix that the auditor found."
  - "2026-08-04 design: F-024 solved with a port — TASK-009 declares AUTH_RATE_LIMIT_PORT and binds a real in-process LocalAuthRateLimiter in wave 2; TASK-051 rebinds RedisAuthRateLimiter to the same token in wave 10. Required at boot, not @Optional(): a missing branding port degrades a 404, a missing limiter opens the credential surface."
  - "2026-08-04 design: mechanism change under a bundled observation — invitation validation moved from databaseHooks.user.after to a before hook, because an after hook cannot roll back the insert that triggered it. ADR-0021's 'reject the signup' was not implementable as written."
  - "2026-08-04 design: round-3 architect return persisted to work/design-return-r3.md ON ARRIVAL, per the process defect recorded 2026-08-04. Rounds 1-2 remain unrecoverable."
  - "2026-08-04 design: TASK amendments applied by the orchestrator (same mechanical class Juano ruled on for F-026) — TASK-009 paths += apps/api/src/main.ts and contracts += rate-limit.md; TASK-013 Produces now states it owns a before hook, with 'do not relax step 5 to avoid the orphan'. TASK-051 already carried rate-limit.md; no change made."
  - "2026-08-04 design: JUANO RULED — TASK-009 split. TASK-058 created for the auth-surface protection (body cap, IP buckets, email hook, port, LocalAuthRateLimiter, 3 tests), depends on TASK-009, wave 3. STORY-005 gained AC-108..AC-111 because no existing AC covered pre-auth limiting. Plan now 58 TASKs; wave table verified — every TASK in exactly one wave, no orphans."
  - "2026-08-04 design: scoped re-review round 4 dispatched. If clear, the Design gate goes to Juano; if not, fix round 4 hands to a FRESH architect on a stronger model."
  - "2026-08-04 design: scoped re-review r4 verdicted F-024, F-025, F-027 ADDRESSED. Verdict changes-requested on F-028 alone, plus 2 minors."
  - "2026-08-04 design: F-028 — LocalAuthRateLimiter has no memory bound while its sibling LocalRateLimiter is LRU-capped at 10,000 with a named constant. Principals are client IP and sha256(email), both attacker-chosen; an IPv6 /64 is free. Live for waves 2-10 and during any Redis outage, converting that outage into an API OOM-restart loop while the redirect path is already degraded."
  - "2026-08-04 design: F-030 — Better Auth ships its own rate limiter, off in dev and test, ON IN PRODUCTION BY DEFAULT, unaccounted for anywhere. If it fires its body carries no code rate_limited and the login screen shows the generic error, which is what F-027 existed to prevent. Auditor asserts this from knowledge, not verification."
  - "2026-08-04 LADDER DEVIATION, ruled by Juano: round 4 resumes the incumbent architect rather than dispatching a fresh one on a stronger model. Reasoning — the ladder's trigger is failure-to-fix, and 30/30 findings have been addressed with every round independently confirmed; each round's findings are new surfaces created by the previous fix, not recurrences. RETRO ITEM: should the trigger be worded as recurrence rather than round count?"
  - "2026-08-04 PROCESS DEFECT CORRECTED: my round-3 return extraction took the LONGEST assistant block rather than the LAST, so work/design-return-r3.md held the original design return. The auditor caught it. Re-extracted correctly, and ALL FOUR returns (r0-initial, r1, r2, r3) are now on disk — rounds 1-2 were recoverable after all. I had reported them lost. Both claims were wrong and are corrected here."
  - "2026-08-04 design: TASK-013's Approach corrected — it still claimed the tenant is created in the same transaction as the user, contradicting the amendment note added below it. My error, not the architect's. databaseHooks.user.after cannot join the transaction that inserted the user."
  - "2026-08-04 design: rework round 4/5 dispatched — F-028, F-029, F-030 plus 4 bundled observations."
  - "2026-08-04 design: rework round 4/5 complete. F-028, F-029, F-030 addressed plus 4 bundled observations. On F-029 the architect went beyond the required change — added AuthGuard step 6, a claim-shape check on sub and tid, which is what actually converts the orphan failure from a 500 into a 401."
  - "2026-08-04 design: F-030 VERIFIED rather than assumed. Better Auth's built-in limiter is confirmed enabled-in-production by default, 60s/100req, in-memory. Disabled via rateLimit:{enabled:false}. Second reason found that the auditor did not have: its header is X-Retry-After, not Retry-After, so even the header path would not be read by apiClient."
  - "2026-08-04 design: ARCHITECT SELF-FOUND DEFECT, no finding covered it. Under ADR-0014's BFF topology the browser never reaches Fly, so Fly-Client-IP is identical for every user and all four IP-keyed buckets collapse into one — 3 signups/hour and 10 sign-ins/5min ACROSS THE ENTIRE PRODUCT. A product outage that would have surfaced only once the BFF and the limiter deployed together, after both passed review. Attributed to itself: introduced with F-004, widened by F-018."
  - "2026-08-04 design: fix is X-Shortkit-Client-IP + X-Shortkit-Proxy-Auth with a constant-time secret match, falling back to Fly-Client-IP. This contradicts F-009 (never trust a client-supplied IP header) and the architect flagged it for the re-review to check specifically. Claims click events are untouched because custom domains CNAME to Fly directly and never traverse the BFF."
  - "2026-08-04 design: scoped re-review round 5 dispatched. THIS IS THE LADDER CAP. If changes-requested, stop dispatching and adjudicate every survivor with a written ruling; load-bearing survivors go BLOCKED and escalate to Juano. Auditor asked to classify each finding as load-bearing / real-but-self-contained / contestable."
  - "2026-08-04 design: NEW ITEM FOR JUANO — BFF_PROXY_SECRET is a shared secret across two deployables and a mismatch degrades SILENTLY into one shared bucket rather than failing loudly. Architect said it disliked this most and could not remove it without risking a boot failure on a misconfigured deploy."
  - "2026-08-04 design: rework round 5/5 by a FRESH architect on a stronger model, per the ladder. All 5 findings addressed. It also ran tsc --noEmit --strict on the touched stubs — four prior rounds had called compilation impossible. Orchestrator reproduced the check independently on the resolver stub: exit 0. First compile-verified artifact in the phase."
  - "2026-08-04 design: LADDER CAP REACHED after 5 fix rounds. Final re-review returned changes-requested with 2 new majors, BOTH classified (b) real-but-self-contained by the auditor. Neither load-bearing, so neither BLOCKED. Adjudicated per superpowers-map.md: F-036 and F-037 PARKED with written rulings, and the warnings carried into TASK-009 and TASK-058 so an implementer meets them where they will do the work."
  - "2026-08-04 design: the round-4 ladder deviation was WRONG and is recorded as such. I argued the trigger is failure-to-fix and nothing had failed; the incumbent had in fact twice reported work it had only partly propagated (round 2's over-claimed AC-84 sentence, round 4's unpropagated BFF fix). The escalation to a fresh architect resolved in one round what four resumes had not."
  - "2026-08-04 design: GATE APPROVED by Juano. 23 ADRs, 20 contracts, 28 stubs, 37 findings (35 fixed, 2 parked). Phase advanced to test."
  - "2026-08-04 NOTE: all work so far is on main. git.branch_prefix is sdlc/ and commits must carry TASK ids from Implement onward — the initiative branch is created when the first TASK dispatches, not before."
  - "2026-08-04 test: phase entered. sdlc-scout skipped a THIRD time — repo still holds 3 files outside .sdlc/ (2 agent defs + .gitignore). Documented in design/test-strategy.md, not silent."
  - "2026-08-04 test: design/test-strategy.md written. 107 ACs allocated across 3 layers (unit / integration / k6 performance) per ADR-0001 and ADR-0018. 3 stories (003, 019, 020) are integration-only — RLS cannot be proven against a mock."
  - "2026-08-04 test: BLOCKED before any sdlc-test-architect dispatch. Step 3 (verify red for the right reason) is unexecutable — there is no vitest, no package.json, no node_modules, so every test written today fails on import, which the phase explicitly rejects as proving nothing AND which is unfalsifiable. TASK-001 is the runner's producer and cannot itself be preceded by a failing test."
  - "2026-08-04 test: plan.md's wave table already has wave 0 = TASK-001 alone + a hard ⛔ GATE re-running /juano-sdlc init. What it does not settle is whether wave 0 runs before or after the Test gate. It has to run before. Escalated to Juano — needs a bootstrap ruling plus test_exempt say-so, which phases/test.md step 4 requires explicitly."
  - "2026-08-04 test: F-038 filed — docker-compose.test.yml has NO producer. ADR-0001 requires it; TASK-001 excludes 'Database' by name; TASK-005's paths do not reach it. Without it pnpm test:integration cannot run locally and 8 of 21 stories are integration-only."
  - "2026-08-04 test: F-039 filed — the CI `integration` job has NO producer. ADR-0001's follow-ups assign it to TASK-002 but TASK-002's Produces block still lists only the `quality` job. Worst-case shape: pnpm test is DB-free and stays green, so CI passes while the entire RLS surface goes unexercised and SC-1 reads as proven by a suite nothing invokes."
  - "2026-08-04 test: root cause shared by F-038/F-039 — ADR 'Follow-ups this creates' sections were never propagated into TASK files. Design's contracts: injection pass DID propagate to all 57 TASKs; nobody treated follow-ups as a second propagation source. Retro item."
  - "2026-08-04 test: OPEN QUESTION for the gate — ADR-0001 cites 'browser-level checks' for async RSC coverage, but no ADR chose a browser tool, no TASK produces one, no AC requires one. Does not block: unit+integration cover all 107 ACs. Either it is manual (belongs in 'Deliberately not automated') or it needs an ADR + TASK."
  - "2026-08-04 test: DEFECT IN OUR OWN LEDGER — findings.yaml has never parsed as YAML. Four unquoted scalars contain ': ' (F-009's 'javascript: and data:' and 'https:', F-023's 'assertTenant(min: TenantRole)', F-030's 'code rate_limited'), which YAML reads as mapping keys. Plan logged config.yaml as machine-validated; findings.yaml never was. Converted the 4 to >- block scalars and verified all four round-trip byte-identical. File now parses: 39 findings, F-001..F-039, 35 fixed / 2 parked / 2 escalated. Retro item: validate every .sdlc/ YAML on write, not just config.yaml."
  - "2026-08-04 test: JUANO RULED on all four escalations. (1) BOOTSTRAP: wave 0 runs BEFORE the Test gate. TASK-001 marked test_exempt: true with reason in front-matter, per phases/test.md step 4 which requires his explicit say-so. Exemption covers TASK-001 alone. Order is TASK-001 -> re-run /juano-sdlc init -> Test phase proper for TASK-002..058 -> Test gate."
  - "2026-08-04 test: (2) F-038 RULED — docker-compose.test.yml assigned to TASK-005. paths gained docker-compose.test.yml; Out-of-scope amended by the orchestrator. TASK-001 rejected as home because it excludes 'Database' by name."
  - "2026-08-04 test: (3) F-039 RULED — integration job assigned to TASK-002. Produces gained the job (pnpm test:integration, postgres:17-alpine service, migrations first, frozen lockfile); Out-of-scope now states it is in scope. AC-5 WIDENED in STORY-002 to require both jobs — id unchanged, STORY-002 still has 3 ACs, nothing renumbered. sdlc-product-auditor must verify both jobs."
  - "2026-08-04 test: (4) browser-level checks RULED manual. Moved into test-strategy.md 'Deliberately not automated'. No ADR, no TASK, no new scope. unit+integration already cover all 107 ACs."
  - "2026-08-04 test: all 4 amendments applied by the orchestrator and committed. findings.yaml now 37 fixed / 2 parked, 0 escalated."
  - "2026-08-04 test: branch sdlc/launch-core created off main at cee06e4. First TASK dispatch, so first branch, per git.branch_prefix."
  - "2026-08-04 test: TASK-001 dispatched to sdlc-implementer-backend (opus). sdlc-scout skipped a FOURTH time — nothing to scout, the TASK creates the workspace. Dispatch carried two explicit overrides: (a) the agent's standing 'never touch apps/web/**' rule, waived by TASK-001's own ⚠ Ownership exception; (b) it writes PASSING tests not red ones, per the test_exempt ruling."
  - "2026-08-04 test: TASK-001 returned DONE_WITH_CONCERNS. Commit f58f340, 37 files, 5150 insertions. Stack pinned: pnpm 11.20.0, node >=22.12.0, vitest 3.2.7, typescript 5.9.3, eslint 9.39.5, NestJS 11.1.28, Next 16.3.0."
  - "2026-08-04 test: ORCHESTRATOR VERIFIED GREEN INDEPENDENTLY, not on the implementer's claim. Deleted all node_modules/.next/dist first, then pnpm install --frozen-lockfile + lint + typecheck + test + build, all exit 0 from that clean state. AC-1 holds. 15 tests / 4 files across all three workspaces, so AC-3 holds (api 2 specs, web 1, contracts 1)."
  - "2026-08-04 test: AC-4 verified in BOTH directions by the orchestrator, not taken on report. Renamed not_found -> resource_not_found in packages/contracts/src/errors.ts: typecheck exited 2 with two TS2339 in apps/web. Reverted; typecheck back to 0; working tree clean. The cross-workspace type edge is real."
  - "2026-08-04 test: GC-4 checked on the commit — zero matches for co-authored-by / generated with / claude / robot emoji."
  - "2026-08-04 test: F-040 filed (implementer-raised, MAJOR). ADR-0018 and ADR-0013 both assign the exact better-auth pin AND the obligation to re-verify 4 Better Auth facts to 'TASK-001's pinning step'. TASK-001 excludes auth by name and has no such step. The implementer declined rather than pin blind — it has no doc-fetch tool, and ADR-0018 says a change in fact (2) or (3) means F-019's and F-021's mechanisms need revisiting BEFORE the pin lands. Third instance of the ADR-follow-up propagation defect after F-038/F-039."
  - "2026-08-04 test: F-041 filed (minor). AC-2 says `pnpm -r list --depth -1` lists 'exactly' three workspaces; it prints a fourth line for the workspace root. Orchestrator VERIFIED --no-include-workspace-root does not suppress it, so no flag satisfies the literal reading while AC-1 still needs root scripts. Needs an AC amendment, not a code change."
  - "2026-08-04 test: implementer's concern about config.yaml's ADR-0005 'generated types' comment CHECKED and found stale — the comment was already corrected on 2026-08-03 and reads 'no generated client and no OpenAPI document'. No action."
  - "2026-08-04 test: TASK-001 left at status tests-green, NOT done. Its four-auditor panel has not run. The state machine allows this (tests-green -> in-audit -> done); the audit belongs to Implement unless Juano wants it pulled forward."
  - "2026-08-04 test: ⛔ INIT GATE CLEARED. config.yaml testing:/quality: populated from commands actually RUN from a deleted-node_modules state, not read off a manifest. command/unit: pnpm test; integration: pnpm test:integration; framework: vitest; lint/typecheck/build: pnpm <name>. coverage_gate stays null — no coverage tooling installed and no threshold chosen, and init.md says a null gate beats a guess."
  - "2026-08-04 test: integration recorded WITH a caveat comment — it currently collects zero test files and passes via passWithNoTests. init.md step 2 says a command collecting nothing is broken rather than working; recorded anyway because ADR-0001 fixes the name and TASK-002 wires CI to it. Becomes real at TASK-005 (compose file) and TASK-006 (harness)."
  - "2026-08-04 test: F-041 RULED — AC-2 amended in STORY-001 to name the three @shortkit/* packages and state the workspace-root line is expected. Id unchanged, STORY-001 still 5 ACs."
  - "2026-08-04 test: F-040 RULED — better-auth pin AND the four-fact re-verification move to TASK-009. TASK-009's Approach gained the full pinning step, the four facts verbatim from ADR-0018, and a STOP-AND-ESCALATE instruction if fact (2) hooks.before/createAuthMiddleware or (3) ctx.body.email has changed, because ADR-0018 says F-019's and F-021's mechanisms would need revisiting and that is a design decision. Applied by the orchestrator (rule 0: tasks/** have no agent owner)."
  - "2026-08-04 test: F-040's ADR half ROUTED, not self-fixed — sdlc-architect dispatched to re-attribute ADR-0018 and ADR-0013 from TASK-001 to TASK-009, told to grep both files for TASK-001 afterwards and to name (not silently fix) any third artifact carrying the same stale attribution. Rule 0: design/** belongs to the architect."
  - "2026-08-04 test: TASK-001 AUDIT PANEL dispatched on Juano's ruling to audit now rather than defer to Implement — 57 TASKs build on this foundation and a finding that moves the vitest config after red tests are written would rework those tests. All 4 auditors concurrent (read-only, safe): sdlc-reviewer (opus), sdlc-security-auditor (opus), sdlc-product-auditor (sonnet), sdlc-design-auditor (sonnet). Review package at .superpowers/sdd/TASK-001/review-cee06e4..f58f340.diff, 1 commit / 182028 bytes, passed as a PATH so the diff never enters orchestrator context. TASK-001 status -> in-audit."
  - "2026-08-04 test: no pre-judging in any auditor dispatch. Each was given findings.yaml and told F-040/F-041 need no re-filing but that they may contest either ruling. The security auditor was explicitly invited to say whether F-040's ruling leaves a real exposure, since it raised the supply-chain requirement originally."
  - "2026-08-04 test: sdlc-design-auditor returned APPROVED, 0 findings, on apps/web/app/{layout,page,not-found}.tsx — the only user-facing surface in the diff. No CSS or font import exists anywhere, metadata present, single-landmark semantic markup, no interactive elements. It considered and deliberately did NOT raise not-found.tsx rendering a raw '404' as its h1, on the grounds that the brief names TASK-046 as the branded-404 owner; noted as context for TASK-046 instead. Correct call — a manufactured finding on acknowledged placeholder scaffolding costs a rework round on the foundation."
  - "2026-08-04 test: ORCHESTRATOR ERROR, corrected — I first reported the design auditor had claimed a report path it never wrote. Wrong. An earlier `cd .sdlc/launch-core` had persisted in the shell, so my existence check ran against a doubled path. The file was on disk the whole time (77 lines). Verified again from an absolute path. Lesson recorded: invariant 9 checks must use absolute paths, or the check itself becomes the defect."
  - "2026-08-04 test: F-040 ADR half COMPLETE. sdlc-architect re-attributed adr-0018 (line 91 + the follow-up bullet at 174-180) and adr-0013 (the rateLimit follow-up bullet at 326-328) from TASK-001 to TASK-009, adding a dated provenance note in each. Orchestrator VERIFIED the grep independently: 3 surviving TASK-001 hits, all provenance or historical (adr-0018:66 is F-016's record of what was grepped), no live attribution of the pin remains. Architect confirmed the ADR wording agrees with TASK-009's amended Approach in both directions."
  - "2026-08-04 test: architect named 6 artifacts carrying the OLD attribution and correctly did NOT rewrite them — findings.yaml, state.yaml, two design audits, two design returns, and TASK-001's own report. Historical records preserving the old state is right; rewriting them would destroy the audit trail."
  - "2026-08-04 test: architect surfaced 2 items OUTSIDE the ruling, both VERIFIED by the orchestrator before acceptance. (a) ADR-0013's follow-up list at 298-313 still says 'TASK-009 owns the entire auth surface, including its rate limiting' and the file contains ZERO occurrences of TASK-058 — confirmed by grep — though Juano split exactly that surface out on 2026-08-04. Same defect class as F-038/F-039/F-040 and NOT covered by parked F-037, which is about rate-limit.md's table. (b) TASK-001 pinned EVERY dependency exactly — confirmed: 0 caret ranges across all 4 package.json files — while ADR-0018 line 83 says 'Everything else keeps caret ranges; the lockfile is what makes them reproducible.' A silent deviation from an accepted ADR."
  - "2026-08-04 test: sdlc-product-auditor returned APPROVED — 0 blocker / 0 major / 0 minor / 1 nit. All 5 ACs met. It verified AC-1 in a GENUINE clean git clone at /tmp/tk-clean rather than in place, which is stronger evidence than the orchestrator's own deleted-node_modules run. It read all four spec files and confirmed none is a placeholder assertion (AC-3 real, not just non-zero: contracts 9 tests, api 4 across two files, web 2)."
  - "2026-08-04 test: product auditor was HONEST about the one thing it could not do — AC-4's negative direction was not re-executed because the auditor is read-only and its attempted edit was correctly blocked by the permission system. It cross-referenced the implementer's transcript against committed file line/columns instead and said so, rather than claiming a verification it did not perform. The orchestrator HAD independently re-executed that direction earlier (TS2339 x2, exit 2, reverted clean), so AC-4 is covered by direct evidence."
  - "2026-08-04 test: product auditor confirmed scope held — exactly 2 of 29 design stubs materialised (errors.ts byte-identical to the stub; index.ts comments the not-yet-existing re-exports with // TASK-007 markers), apps/web/src/** absent so the TASK-008/012 boundary is intact, and none of database / Redis / auth / deploy config / CI workflow / domain schema was built."
  - "2026-08-04 test: product auditor NIT — .gitattributes is genuinely assigned to TASK-001 by ADR-0004 but TASK-001's own paths frontmatter never names it, so a reader checking scope against the TASK file alone would flag it as unexplained. Documentation-only, no code change."
  - "2026-08-04 test: sdlc-security-auditor returned CLEAR — 0 blocker / 0 major / 4 minor / 2 nit. It ran pnpm audit --prod --audit-level high itself: exit 0, no known vulnerabilities. 470 locked resolutions, all sha512, all npm registry, no git/tarball/http sources, no overrides or patchedDependencies, no secrets in the diff, no tracked key material. allowBuilds correctly scoped to @swc/core and esbuild, both dev-only. Licenses: 298 MIT / 25 Apache-2.0 / 17 ISC / 13 BSD + one LGPL-3.0-or-later (sharp via next) — not a distribution trigger for a hosted service. It also wrote its report via bash heredoc because it has no Write tool."
  - "2026-08-04 test: sdlc-reviewer returned CHANGES-REQUESTED — 1 major / 4 minor / 2 nit. It has NO Write tool and its instructions forbid writing reports, so it returned the review inline; the orchestrator persisted it verbatim to audits/TASK-001-sdlc-reviewer-r1.md. WORKFLOW DEFECT: implement.md and the dispatch template both tell auditors to write their own audit file, but 3 of the 4 auditor slots ship without Write. Two worked around it with bash; sdlc-reviewer correctly refused. Retro item."
  - "2026-08-04 test: F-042 MAJOR — apps/api has no working build or runtime path to @shortkit/contracts. ORCHESTRATOR REPRODUCED IT IN-REPO rather than accepting the auditor's scratch project: a probe file importing ERROR_CODE_STATUS gives typecheck EXIT=0, test EXIT=0, build EXIT=2 with two TS6059 'not under rootDir'. Two gates green, one red. The import is certain, not hypothetical — ADR-0005 and errors.ts's own header put the error table in the API exception filter, landing wave 1 or 2."
  - "2026-08-04 test: F-042 second half found by the orchestrator, not the auditor — the FAILED build emitted errors.js/.js.map and index.js/.js.map into packages/contracts/src/ and NONE is gitignored. A `git add -A` after a failed build commits compiler output into the contracts package. Cleaned up; folded into F-042's required_change and F-048's."
  - "2026-08-04 test: F-045 also REPRODUCED IN-REPO — `import { RESERVED_SLUGS } from '@shortkit/contracts/slug'` gives typecheck EXIT=2 TS2307. The exports map says ./src/*/index.ts; the stubs are flat files. Every subpath the map advertises is dead."
  - "2026-08-04 test: LEDGER DEFECT IN MY OWN WORK, caught by sdlc-reviewer. F-040 and F-041 still read status: escalated with resolved_by: null and no ruling block, while STORY-001 already carried the amended AC-2 text — so F-041's fix was applied to the artifact but never written back to the ledger, and F-040's ruling existed only in a dispatch prompt. findings.yaml is declared append-only and IS the record of what was decided. Both now carry status: fixed plus written rulings. The reviewer agreed with both rulings on substance."
  - "2026-08-04 test: 14 findings filed, F-042..F-055. 1 major / 10 minor / 3 nit. The two nits both the reviewer and the security auditor raised independently (PORT empty-string, engines.node vs @types/node) are DEDUPED into F-051 and F-052 with source recorded as both."
  - "2026-08-04 test: routing resolved by the orchestrator per routing.md, auditors never assign owner_slot. sdlc-test-architect: F-043 (a test file, rule 0). sdlc-implementer-backend: F-044, F-046, F-047, F-048, F-051, F-052. sdlc-architect: F-045, F-049, F-054 (design/**, rule 0). Juano: F-042, F-050, F-053, F-055 — three touch approved artifacts and F-042 contradicts ADR-0005's alternatives table, which implement.md says is his call and not a fix."
  - "2026-08-04 test: TASK-001 status -> rework. One major open. Per implement.md a blocker/major bars status: done."
  - "2026-08-04 test: JUANO RULED on all 4 audit escalations. F-042 -> bundle apps/api with swc/tsup, inlining workspace source; the dist-build option was REJECTED, which keeps ADR-0005's no-build-step-for-contracts decision intact and avoids the stale-dist mode the ADR feared. F-055 -> keep exact pinning everywhere and amend ADR-0018 to say so, paired with an update mechanism. F-050 -> mint an AC. F-053 -> widen TASK-001's paths."
  - "2026-08-04 test: F-050 APPLIED — AC-112 minted on STORY-005 (now 11 ACs) requiring apps/api/package.json to declare better-auth with no range character AND TASK-009's report to record the four ADR-0018 facts verified against that release. TASK-009 acceptance is now [AC-16, AC-20, AC-21, AC-112]. This is the gate that makes F-040's fix hold — without it sdlc-product-auditor verifies ACs verbatim and never checks the pin."
  - "2026-08-04 test: F-053 APPLIED — TASK-001 paths widened to include pnpm-lock.yaml, vitest.config.ts, .gitattributes and eslint.config.mjs, all required by ADR-0001/ADR-0004/ADR-0018 and none previously listed. Documentation only; the product auditor had already verified the TASK stayed in scope."
  - "2026-08-04 test: FIX ROUND 1/5 for TASK-001. FIX_BASE=0e7b3b2. Staged deliberately rather than dispatched all at once — the architect's F-045 shape decision is an input the implementer needs, so design goes first and code second."
  - "2026-08-04 test: stage 1 dispatched, 2 agents concurrent on provably disjoint paths. sdlc-architect gets F-045/F-049/F-054/F-055 and writes design/** ONLY — told explicitly not to touch packages/contracts/package.json, either tsconfig, anything under apps/, or any TASK/STORY file. sdlc-test-architect gets F-043 and owns exactly one file, apps/api/src/app.module.spec.ts."
  - "2026-08-04 test: the architect was given F-042's ruling as CONSTRAINT, not as its own finding — its F-045 shape has to work under a bundler-based API build and Bundler resolution on the web side. It was also asked to record in ADR-0005's Consequences that 'pnpm workspaces plus TS path mapping make the build unnecessary' is now known false for the API's tsc build, since the decision itself stands."
  - "2026-08-04 test: test-architect told to LEAVE decorator-metadata.spec.ts alone — sdlc-reviewer explicitly disagreed with the implementer's own offer to delete it, on the grounds that it is the only thing converting ADR-0001's silent unplugin-swc failure mode into a loud one. Also told this is not a red-test dispatch: TASK-001 is test_exempt and the suite must stay green."
  - "2026-08-04 test: stage 2 (sdlc-implementer-backend: F-042, F-044, F-046, F-047, F-048, F-051, F-052) is HELD until the architect returns, per one-dispatch-per-owner-per-round."
  - "2026-08-04 test: F-043 FIXED by sdlc-test-architect in round 1. It removed the empty-imports assertion rather than replacing it, and justified the choice: any replacement would be either a transient-count assertion in disguise or an assertion against Reflect.getMetadata, which tests Nest's metadata storage rather than this codebase (writing-good-tests.md 'your code, not the framework'). ORCHESTRATOR VERIFIED: git diff shows only app.module.spec.ts touched, -4 lines; pnpm test 14 passed (was 15, delta is exactly the deleted assertion); api still has 3 tests across 2 files so AC-3 holds in all three workspaces."
  - "2026-08-04 test: it also confirmed the surviving test is not hollow — naming the production change that would fail it (breaking AppModule's Nest compilation, or a later unresolvable provider graph once feature modules register). That was asked for explicitly because a test left behind after deleting its sibling is where hollow assertions hide."
  - "2026-08-04 test: sdlc-architect returned; F-045, F-049, F-054, F-055 all addressed in 3 accepted ADRs, no new ADRs, no files outside design/**. ORCHESTRATOR VERIFIED: adr-0005 carries the new exports block, adr-0018 carries audit-level moderate + the weekly dependencies job + exact-pinning-as-stance + dependabot, adr-0013 went from 0 to 13 occurrences of TASK-058."
  - "2026-08-04 test: F-045 SHAPE DECIDED — root specifier only. exports: { '.': './src/index.ts' } plus sideEffects: false; the '@shortkit/contracts/*' tsconfig key is DELETED in both apps, not rewritten, so a subpath import fails at typecheck rather than resolving one way in the bundler and another in tsc. Architect's reasoning: zero subpath consumers exist anywhere in the design, the stub layout has a nested file (domains/reserved-hostnames.ts) that a flat mirror still would not resolve, and a single '.' entry behaves identically under node10, node16, Bundler and esbuild — which is what makes it safe under F-042's swc/tsup build. design/stubs needs NO relaying; verified already consistent."
  - "2026-08-04 test: sideEffects: false was added beyond the three-place edit the finding described, and the architect recorded its own cost — nothing enforces the assertion, so a contract module that ever runs code at import time gets silently dropped from the bundle. Contracts must stay declaration-only. Carried into TASK-007's constraint block."
  - "2026-08-04 test: architect flagged TWO THINGS IT COULD NOT DO, both real. (a) .github/dependabot.yml had NO OWNER — TASK-002's paths were .github/workflows/**, which excludes it, so the update mechanism Juano's F-055 ruling requires could not be written by anyone. (b) TASK-009's Produces never mentioned the beforeHooks array ADR-0013 says it creates. Both are TASK edits, which the architect correctly refused to make."
  - "2026-08-04 test: ORCHESTRATOR APPLIED both, as the mechanical consequence of rulings Juano already made (same class as F-053). TASK-002 paths .github/workflows/** -> .github/**, with the reason recorded in the TASK. TASK-009 gained a composition-constraint block (creates beforeHooks EMPTY; TASK-058 and TASK-013 append, neither replaces) and the contracts import rule. TASK-007 and TASK-024 gained the normative entry-point block; TASK-058 gained its composition boundary."
  - "2026-08-04 test: F-056 FILED — ADR-0013 contradicts itself on who maps Better Auth's native error bodies onto ErrorEnvelope. Line 290 says TASK-009 at the web client boundary; line 332 says TASK-008. ORCHESTRATOR VERIFIED both lines AND both TASKs' paths: TASK-009 cannot write apps/web at all, so line 290 is impossible rather than merely ambiguous. TASK-008 owns apps/web/src/lib/api/** and components/errors/**, exactly where it belongs. Routed to sdlc-architect. Found by the architect while fixing F-054 and correctly left unedited as outside its brief."
  - "2026-08-04 test: architect noted TASK-009 and TASK-058 both hold write access to apps/api/src/auth/auth.config.ts. depends_on: [TASK-009] sequences them so it is not a wave conflict, but it is two TASKs writing one file. Recorded in both TASK files."
  - "2026-08-04 test: architect left ADR-0005's alternatives-table sentence 'pnpm workspaces plus TS path mapping make the build unnecessary' VERBATIM and put the correction in Consequences, per instruction, then flagged that a reader hitting the table first meets a claim the Consequences later qualify. Honest ordering, uncomfortable one. Noted, not changed — the decision stands and rewriting history in an accepted ADR is worse."
  - "2026-08-04 test: ORCHESTRATOR ERROR IN THE LEDGER, caught and corrected before commit. My first attempt to write the round-1 rulings used a regex with (?:.*\\n)*? that slid ACROSS finding boundaries — F-042's ruling landed on F-044 and F-044's on F-045. Reverted findings.yaml via git checkout and redid it by splitting the file into per-finding blocks on a column-0 '- id: ' anchor, so an edit cannot cross a boundary, with an assert that each block actually changed. Verified after: every id carries its own ruling. Same class as the earlier cd-persistence error — the verification step is where my defects are landing, not the work."
  - "2026-08-04 test: FIX ROUND 1/5 COMPLETE for TASK-001. 14 of 15 findings fixed; F-056 open (raised during the round). Nothing parked, nothing discarded."
  - "2026-08-04 test: sdlc-implementer-backend returned DONE_WITH_CONCERNS with ONE DISPUTED REMEDY, and the dispute was accepted on evidence rather than on argument. It refused nodenext for F-046 because nodenext forces .js extensions in the ESM contracts source, Turbopack then cannot resolve ./errors.js -> errors.ts, pnpm build goes red for apps/web, and it contradicts the frozen TASK-007 stub. It chose module Preserve / moduleResolution Bundler instead. ORCHESTRATOR TESTED THE UNDERLYING DEFECT: reflect-metadata/lite (exports-only subpath) gives TS2307 under node10 and exit 0 under Bundler. The property F-046 named is fixed. Bundler is also correct now that Juano ruled apps/api is built by tsup."
  - "2026-08-04 test: F-042 VERIFIED FROM A CLEAN INSTALL, all six commands exit 0 after deleting node_modules/.next/dist. node apps/api/dist/main.js prints 'Nest application successfully started'. Emitted requires are ONLY @nestjs/common, @nestjs/core, reflect-metadata — zero .ts requires, so the ERR_UNKNOWN_FILE_EXTENSION risk on the engines floor is closed. slug_generation_exhausted appears in dist/main.js, proving contracts is inlined as JavaScript."
  - "2026-08-04 test: implementer respected the test-file boundary — git show over its commit a3221b9 confirms it did NOT touch app.module.spec.ts (that was the test-architect's 96d2d54). It did edit vitest.integration.config.ts, which is F-044's assigned config file, not a test."
  - "2026-08-04 test: TWO ITEMS FOR JUANO out of the fix round. (1) F-052 was fixed by raising engines.node to >=24.13.0, which DROPS Node 22 support — a deployment-floor decision, not the type alignment the finding asked for. (2) apps/api/src/common/error-envelope.ts is NEW production code no AC names; the implementer flagged it itself, and it exists because MY OWN acceptance criteria for F-042 demanded a real apps/api/src -> contracts import as evidence. Both go to the scoped re-review before the gate."
  - "2026-08-04 test: F-056 FIXED by sdlc-architect. ADR-0013 line 290 now names TASK-008; the mechanism claim around it is untouched. VERIFIED by reading the line."
  - "2026-08-04 test: architect then audited ALL 13 remaining TASK-009 occurrences in adr-0013 against the current split and found one more, F-057. ADR-0013 assigns TASK-009 'an e2e test asserting POST /api/auth/sign-up/email receives a parsed body'. VERIFIED: that is integration-layer per test-strategy.md, TASK-009's paths contain no apps/api/test/** at all, and TASK-058 is the TASK holding apps/api/test/auth/**. The ADR is the only place the obligation exists and it names a TASK that cannot discharge it."
  - "2026-08-04 test: F-057 ESCALATED rather than routed. The architect declined to choose and was right to: the parsed-body assertion is the acceptance signal for TASK-009's own mount, so moving it to TASK-058 ships the mount one wave unverified. That is an ownership call with a wave consequence, not a mechanical re-attribution."
  - "2026-08-04 test: SIXTH instance of one defect class — F-038, F-039, F-040, F-054, F-056, F-057 are all an ADR assigning work to a TASK that cannot do it, or naming an owner the TASK files contradict. Design's contracts: injection pass propagated to all 57 TASKs; ADR follow-up lists and consequence bullets were never treated as a propagation source. This is now the single largest recurring defect in the initiative and belongs in Retro as a mechanical check, not a discipline note."
  - "2026-08-04 test: SCOPED RE-REVIEW dispatched over the fix diff ONLY, package .superpowers/sdd/TASK-001/review-0e7b3b2..5054a03.diff (6 commits, 151661 bytes), passed as a PATH. Both auditors that raised findings re-dispatched concurrently: sdlc-reviewer verdicts F-042/043/044/045/046/051/052, sdlc-security-auditor verdicts F-047/048/049/050/051/052."
  - "2026-08-04 test: both re-review dispatches name the two things the fix round produced that nobody asked for, rather than hiding them — apps/api/src/common/error-envelope.ts is new production code with no AC and no test, and main.ts routes its startup failure through the envelope, which is what puts the contracts import in the bundle graph. The implementer invited a challenge on the second and the dispatch passes that invitation through."
  - "2026-08-04 test: sdlc-reviewer was told explicitly it is FREE TO DISAGREE with my acceptance of the F-046 dispute, and asked to name a concrete gap Bundler leaves that node16 would have closed if it sees one. Rulings Juano made are off-limits for re-argument, but a ruling's bad consequence may be filed as a NEW finding — the distinction is stated in both dispatches so neither auditor self-censors."
  - "2026-08-04 test: the security auditor was given the tsup dependency growth as an explicit supply-chain question — what entered the tree, any lifecycle scripts, whether allowBuilds still scopes postinstall correctly, and whether every resolution still carries sha512 from npm. It confirmed 470 clean resolutions in round 1, so the delta is checkable."
  - "2026-08-04 test: also asked whether gitignoring .npmrc WHOLESALE is right. The implementer skipped engine-strict=true because F-048's fix made .npmrc uncommittable — a fix constraining a later fix, worth a second opinion rather than acceptance."
