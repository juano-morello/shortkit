# The Postgres and the Redis `pnpm test:integration` runs against locally.
#
# Contract: docs/contracts/rls-policy-template.md (roles, grants, env vars),
#           docs/contracts/redirect-cache.md (the namespace every key begins with)
# ADR: adr-0001-test-framework.md, adr-0003-rls-policy-template-and-roles.md,
#      adr-0012-redis-client-and-rate-limit-degradation.md
# Produced by: TASK-005 (F-038). The `redis` service added by TASK-2-04 (D-2-16).
#
#   docker compose -f docker-compose.test.yml up -d --wait
#   export DATABASE_URL='postgres://shortkit_app:app@127.0.0.1:55433/shortkit_test'
#   export DATABASE_MIGRATION_URL='postgres://shortkit_migrator:migrator@127.0.0.1:55433/shortkit_test'
#   export DATABASE_AUTH_URL='postgres://shortkit_auth:auth@127.0.0.1:55433/shortkit_test'
#   export REDIS_URL='redis://:redis-fixture@127.0.0.1:56380'
#   export REDIS_KEY_NAMESPACE=test
#   pnpm test:integration
#   docker compose -f docker-compose.test.yml down -v
#
# REDIS_KEY_NAMESPACE is required whenever REDIS_URL is set: the process refuses to
# boot rather than defaulting to a namespace that might collide (redirect-cache.md).
# `test` here, `ci-<run_id>` in CI so two concurrent jobs cannot collide either, `dev`
# in docker-compose.yml. A suite run against a clone that never started this stack
# must SAY that Redis is unreachable rather than hang: `enableOfflineQueue: false` and
# a 50 ms command timeout are what make that true (ADR-0012).
#
# The CI equivalent is a `services:` container in the `integration` job and belongs
# to TASK-002 (F-039), not here. Redis included, with one difference recorded there:
# GitHub's `services:` has no `command:`, so the CI container cannot be given
# `--requirepass` and its DSN carries no password.
#
# 55433 rather than 5432 so a Postgres already running on the developer's machine
# is neither shadowed nor connected to by accident: a test suite that silently
# reaches a real database drops and recreates `tenants`. 56380 rather than 6379 for
# the same reason and with a sharper edge: a suite pointed at a developer's own Redis
# would write and delete keys under a namespace that machine did not choose.
#
# The credentials below are fixtures. They exist in a file that only ever points at
# a throwaway container, and the suite refuses to run against a role that could
# bypass RLS, so there is nothing here to protect.
#
# ---------------------------------------------------------------------------
# TEST-ONLY. DO NOT ADAPT THIS FILE INTO A PRODUCTION DATABASE (F-131).
# ---------------------------------------------------------------------------
#
# It is the repository's only worked example of role provisioning, so it is what
# someone reaches for under time pressure. Half of it is meant to be copied and
# half of it must not be.
#
# Reusable. This is rls-policy-template.md's "Roles" section, applied:
#   - all three roles NOBYPASSRLS, all three with attribute defaults otherwise;
#   - shortkit_app owns nothing and receives DML only through
#     ALTER DEFAULT PRIVILEGES granted by shortkit_migrator; shortkit_auth owns
#     nothing and receives no default privilege at all: its DML is hand-written
#     per table (ADR-0050);
#   - USAGE on schema public and nothing more.
#
# NOT reusable:
#   - literal passwords in a committed file, including the bootstrap superuser's;
#   - no TLS requirement. A production DSN with `sslmode=require` gets encryption
#     with NO certificate verification; `sslmode=verify-full` is the one that means
#     what it looks like;
#   - tmpfs storage, which loses everything on restart;
#   - ALTER DEFAULT PRIVILEGES is scoped to the identity `shortkit_migrator`, so a
#     deploy that runs migrations as any other role grants shortkit_app nothing.
#     That fails closed, but it fails at runtime rather than at deploy.
#
# Provisioning the production roles is not this file's job and is not yet owned by
# any TASK; it is routed alongside F-116.

services:
  postgres:
    image: postgres:17-alpine
    environment:
      # The bootstrap superuser. None of the three DSNs above uses it: every
      # statement the suite issues comes from one of the three NOBYPASSRLS roles,
      # because a superuser is exempt from every policy and would make
      # AC-8..AC-11 vacuous.
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - '127.0.0.1:55433:5432'
    # tmpfs, not a volume: the suite drops and recreates its fixture per test and
    # nothing here is worth keeping between runs. `down` leaves no disk behind.
    tmpfs:
      - /var/lib/postgresql/data
    configs:
      - source: shortkit_roles_sql
        target: /docker-entrypoint-initdb.d/10-roles.sql
    healthcheck:
      # Executes a statement AS shortkit_app, over TCP, against shortkit_test
      # (F-127). All three halves are load-bearing, and `pg_isready` has none of
      # them: it does not authenticate (its -U and -d only affect what the server
      # logs), and with no -h it uses the unix socket. The postgres entrypoint runs
      # initdb, starts a TEMPORARY socket-only server, runs the init script, stops
      # it, then starts the real one. A `pg_isready` probe landing in that window
      # returns 0 and marks the container healthy, so `up -d --wait` returns and the
      # next command gets ECONNREFUSED or "role shortkit_app does not exist". This
      # probe is false until the roles exist, the database exists, and the real
      # server is accepting TCP connections.
      #
      # IT DOES NOT CHECK THE PASSWORD, and this comment used to claim it did
      # (F-357, corrected 2026-08-11). postgres:17-alpine's generated pg_hba.conf
      # puts `host all all 127.0.0.1/32 trust` AHEAD of the `scram-sha-256` line the
      # entrypoint appends, and the first matching line wins, so a connection to
      # 127.0.0.1 from inside this container is trusted and the `app` in the DSN
      # below is never verified. Measured against a running container: a wrong
      # password over 127.0.0.1 was accepted; the same wrong password over the
      # container's own bridge address was refused.
      #
      # The gap is inert here, which is why the probe is left alone. Both passwords
      # are literals, in the configs block below and in the DSNs at the top of this
      # file, with no interpolation anywhere, so there is no mechanism that can make
      # them disagree. The suite's own connections come from the host through the
      # published port and arrive on the container's eth0 rather than its loopback,
      # so the suite authenticates for real even though this probe does not.
      #
      # DO NOT COPY THE `127.0.0.1` INTO A STACK WHOSE PASSWORDS COME FROM THE
      # ENVIRONMENT. There, a probe that skips authentication reports a database
      # with unusable roles as healthy. docker-compose.yml uses `-h postgres` for
      # exactly that reason (ADR-0036). Changing this probe to match is a behaviour
      # change to TASK-005's file and needs that TASK's owner, not a drive-by.
      test:
        - 'CMD-SHELL'
        - 'psql "postgres://shortkit_app:app@127.0.0.1:5432/shortkit_test" -tAc "select 1" >/dev/null 2>&1'
      interval: 1s
      timeout: 3s
      retries: 30
      start_period: 10s

  # The cache the integration suite exercises (ADR-0012, docs/contracts/redirect-cache.md).
  # Same shape as docker-compose.yml's, on its own port, and the fixture password is a
  # fixture in the same sense as the three role passwords above: committed, printed by
  # `docker compose config`, bound to 127.0.0.1, and pointed at a container that is
  # destroyed with `down`. TEST-ONLY, like everything in this file (F-131).
  #
  # No tmpfs and no volume, unlike `postgres` above: Redis holds its dataset in memory,
  # the image's default RDB snapshots land in the container's writable layer, and `down`
  # takes both. Nothing here is worth keeping between runs.
  redis:
    image: redis:7-alpine
    command: ['redis-server', '--requirepass', 'redis-fixture']
    ports:
      - '127.0.0.1:56380:6379'
    healthcheck:
      # Reads PONG off stdout rather than trusting the exit code: redis-cli exits 0 on a
      # server ERROR reply, so an unauthenticated probe against a `--requirepass` server
      # gets `NOAUTH Authentication required.` and would still report healthy. With the
      # grep, this container is healthy only when the password in this file is the password
      # the server has: the property ADR-0036 gives the Postgres probe over in the dev
      # stack, and the one the probe above deliberately does NOT have.
      test:
        - 'CMD-SHELL'
        - 'redis-cli --no-auth-warning -a redis-fixture ping | grep -q PONG'
      interval: 1s
      timeout: 3s
      retries: 30
      start_period: 5s

configs:
  # Inline rather than a mounted file: F-038 assigns TASK-005 `docker-compose.test.yml`
  # and nothing beside it, and the roles are only meaningful together with the
  # service that runs them.
  shortkit_roles_sql:
    content: |
      -- docs/contracts/rls-policy-template.md "Roles", verbatim but for the
      -- literal passwords, which psql cannot take as :'variables' here.
      --
      -- NOBYPASSRLS on all three roles. It is the default, and it is written out because
      -- a role that can bypass RLS turns every isolation assertion in the suite into
      -- a tautology; test/support/rls-fixture.ts re-checks it before each run.
      CREATE ROLE shortkit_migrator LOGIN PASSWORD 'migrator' NOBYPASSRLS;
      CREATE ROLE shortkit_app      LOGIN PASSWORD 'app'      NOBYPASSRLS;
      -- shortkit_auth: runtime, Better Auth only. Owns nothing (ADR-0050).
      CREATE ROLE shortkit_auth     LOGIN PASSWORD 'auth'     NOBYPASSRLS;

      -- The migrator owns the database, so it owns schema `public` through
      -- pg_database_owner and can run DDL without any further grant. The app role
      -- owns nothing (ADR-0003).
      CREATE DATABASE shortkit_test OWNER shortkit_migrator;

      \connect shortkit_test

      -- A default privilege for shortkit_auth would grant it DML on every table
      -- the migrator creates, including every tenant-scoped one. The split cannot
      -- be expressed as a default privilege in either direction
      -- (rls-policy-template.md "Roles", ADR-0050). shortkit_auth's DML on the five
      -- Better Auth tables is hand-written per table in migration 0001 (TASK-002).
      GRANT USAGE ON SCHEMA public TO shortkit_app, shortkit_auth;
      ALTER DEFAULT PRIVILEGES FOR ROLE shortkit_migrator IN SCHEMA public
        GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO shortkit_app;
      ALTER DEFAULT PRIVILEGES FOR ROLE shortkit_migrator IN SCHEMA public
        GRANT USAGE, SELECT ON SEQUENCES TO shortkit_app;
