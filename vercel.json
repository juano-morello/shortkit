{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile --filter @shortkit/web...",
  "buildCommand": "pnpm --filter @shortkit/web build && pnpm --filter @shortkit/web assert:no-secrets",
  "outputDirectory": "apps/web/.next"
}
